# Aztec node URL (defaults to /aztec which Vite proxies to localhost:8080)
# AZTEC_NODE_URL=http://localhost:8080

# Sponsored FPC salt (for private FPC instances on live networks)
# SPONSORED_FPC_SALT=0x...
