# Plan — Bump `@aztec/* 5.0.0-rc.2 → 5.0.0` (stable) + release the SDK (`/blueprint mid`)

**Tier:** `mid` (rubric: external coupling HIGH; blast radius / irreversibility / security MODERATE; novelty LOW — third bump on this line). **Status: ✅ APPROVED 2026-07-13** (verdict: `approve`; **Ask 1 = fully autonomous** — both P3 dispatches authorized once their gates pass, explicitly overriding the joint-stable default for this plan; **Ask 2 = wallet funded, `BRIDGE_AMOUNT` = 1000 FJ** (rc.2 parity, lessons-verified); **Ask 3 = same-day smoke-gated `latest` flip**). All three audit rounds folded: codex round-1 `reject` → adopted/dispositioned; fable `conditional approve` (5 conditions) → adopted; final fresh-context codex `reject`→(resumed)→`conditional approve`, condition folded.

## Goal

Track Aztec's **5.0.0 stable** release (npm 2026-07-13T09:39Z; the v5 testnet node already reports `nodeVersion: "5.0.0"`) by bumping the 24 `@aztec/*` pins `5.0.0-rc.2 → 5.0.0`, migrating the TS surface the rc.2→stable changelog actually hits, redeploying+funding the 5.0.0-derived SponsoredFPC on v5 testnet (bytecode changed again), **pre-publish-smoking the transfer/tagging path**, publishing the SDK on the **`testnet` dist-tag**, redeploying the playground, and — for the first time — **promoting the SDK to npm `latest`** (timing = Ask 3: same-day smoke-gated vs deferred to 2026-07-20). **SDK-only** model: the deployed accelerator (1.0.6) downloads `bb` at runtime keyed by `x-aztec-version`; the **entire `packages/accelerator/core/` crate is diff-empty `accelerator-v1.0.6..HEAD`** (fable re-verified, stronger than the draft's file-scoped claim), so no accelerator app release unless the P2 e2e reveals interface breakage.

**Done =** SDK published as `5.0.0` on `testnet`; pre-publish + post-deploy smokes green (incl. the released-1.0.6 native path + one Safari pass); 5.0.0 salt=0 SponsoredFPC deployed+funded; live playground on 5.0.0; `latest` promoted per Ask 3's decision; CI green; no accelerator release.

## Breaking-change assessment (changelog → our surface)

| Changelog item | Our exposure |
|---|---|
| **Signing-key-rooted accounts** (`createSchnorrAccount` signing key now REQUIRED) | **HIT ×5** (both auditors corrected the draft's ×3): `packages/playground/src/aztec.ts:473`, `:649`, `packages/sdk/e2e/e2e-helpers.ts:25`, `packages/playground/scripts/deploy-sponsored-fpc.ts:149`, `packages/playground/scripts/batch-fund-fpc.ts:250` all pass `(secret, salt)` only. `@aztec/wallets@5.0.0` signature: `(secret: Fr, salt: Fr, signingKey: Fq, alias?)` — order unchanged, third arg now required. `aztec.ts:242` already passes 3. Each touched file also needs the `Fq` import (`@aztec/aztec.js/fields` exports it — verified; `e2e-helpers.ts:10` currently imports only `Fr`). All our accounts are ephemeral randoms → address changes inert. |
| **Browser KV default → SQLite-OPFS** (via `@aztec/wallets` EmbeddedWallet) | The playground creates **persistent** stores (`aztec.ts:222-228`, no `ephemeral`), and its init-failure recovery clears **IndexedDB only** (`clearIndexedDB`, `aztec.ts:129-145`, retry at `:267`) — a no-op for OPFS state, so the 3-attempt self-heal dies; a second tab hits the origin-wide lock; the comments at `aztec.ts:153` AND `:238` (fable caught both) go stale. **Fix adopted: `ephemeral: true`** (`EmbeddedWalletOptions.ephemeral`, 5.0.0 d.ts: "Use ephemeral (in-memory) stores") — per-session random accounts made persistence worthless; in-memory sidesteps lock, second-tab, and recovery-gap at once, and is STRONGER than patching the recovery path. Keep `clearIndexedDB` as one-time legacy cleanup; fix both comments. *Mechanics nuance (fable F3): whether the OPFS default hits the wallet-DB too is unverified (upstream doc still says IndexedDB for wallet-DB) — downgraded to Inference; under `ephemeral: true` the distinction stops mattering, and P1 verifies ephemeral covers both stores (else pass an explicit in-memory store via `walletDb.store`).* |
| **Non-interactive handshake default** (unconstrained delivery to external recipients) — *fable I1: absent from the draft entirely* | Runtime-**behavioral** change to note tagging/discovery — invisible to grep, tsc, and the account-deploy-only SDK e2e. Lands exactly in the playground's private `transfer(bob, 500n)` (`aztec.ts:713-729`). **Gated by the new pre-publish transfer smoke (P3b)** — the only layer that can see it. |
| **PXE DB reset on upgrade** | Non-event once the playground is ephemeral; SDK e2e state is per-run. |

**Not our surface** (fable independently re-verified zero hits): `MessageContext`→`ResolvedTx`, `TestEnvironmentOptions`, `consume_l1_to_l2_message` arrays (Aztec.nr — no contracts here); `computeL1ToL2MessageNullifier`→`computeFeeJuiceMessageNullifier` (high-level `L1FeeJuicePortalManager`/`FeeJuicePaymentMethodWithClaim` only — `typecheck:scripts` + the real P3a run cover it); `pxe.registerContract` split (`BaseWallet.registerContract(instance, artifact?)` survives in 5.0.0 — verified); `AccountWithSecretKey`, CLI `--signing-key`, bot config (unused). `tsc` = necessary, not sufficient.

**Typecheck coverage gap (codex HIGH, verified):** playground **source** is typechecked by nothing — root `test:typecheck` covers only the SDK (`package.json:27`), `vite build` transpiles without checking, `bun test` doesn't typecheck, and raw `tsc --noEmit` fails today on config gaps (`bun:test` types, `.css` import). Without fixing this, the playground migration sites pass every gate while broken → P1 gains typecheck-enablement (same escape class that bit rc.2 via untyped `scripts/` → #371).

**Precompiled-artifact hit (rc.2 lesson, repeated):** SponsoredFPC artifact sha256 `57b124ba… → 648b856a…` (both recomputed independently by fable) → salt=0 instance address moved → P3a redeploy required.

---

## Approach A′ — Track-now; publish `testnet`; smoke-gate the publish; promote `latest` per Ask 3 (primary)

Bump + migrate now; local-only min-age override for the lockfile regen; land through the native-bb e2e; redeploy FPC; **pre-publish local smoke** (deploy+transfer via `dev:testnet` — fable's condition 2); publish `testnet`; deploy playground; post-deploy acceptance smoke (released-1.0.6 native path + Safari pass); promote `latest` via a new tag-only workflow at the time Ask 3 decides. **Why now:** testnet node already on 5.0.0; upstream `latest` flipped; canonical-FPC address already moved; current `latest` (4.3.1) is already incompatible with the only live network.

## Approach B — Soak-split (competing outline, resolved into Ask 3)

Same everything, but hold ONLY the `latest` promotion until 2026-07-20 (5.0.0's 7-day maturity) as an upstream-compromise soak. Both auditors converged here: publish-timing and promotion-timing are separable, and the promotion is the only irreversibility-adjacent step with a free soak option. Honest counterweight (fable): `latest` today is 4.3.1 — already a broken default install against v5 — so deferring keeps the default broken a week longer; and `testnet`-tag consumers + the playground carry the same artifact same-day either way, so the soak protects only unpinned-`latest` installers. **Genuinely disputed default** (codex-hybrid: same-day; fable: defer) → surfaced as Ask 3, user decides at the gate.

---

## Phases

### P1 ✓ — Bump + migrate + typecheck-enablement + lockfile (local min-age override) — gate passed 2026-07-13

- `bun run aztec:update 5.0.0` — post-#372 "full recipe": rewrites all 24 pins (11 sdk + 13 playground), auto-bumps `CRS_CACHE_VERSION`, auto-pins the Windows bb 5.0.0 checksum (soft-fails with instructions), prints the FPC reminder. **Zero-skip gate** (both auditors; all 13 distinct `@aztec` packages verified published at 5.0.0, so skips = a red flag): tool output reports zero skips AND `grep -rn '"@aztec/' packages/*/package.json` shows every pin exactly `5.0.0`.
- **Migrate all 5 `createSchnorrAccount` sites** (+ `Fq` imports per file, incl. `e2e-helpers.ts`): `aztec.ts:473`, `:649`, `e2e-helpers.ts:25`, `deploy-sponsored-fpc.ts:149`, `batch-fund-fpc.ts:250`. Checkable: `grep -rn createSchnorrAccount packages | grep -v node_modules` shows every call passing a signing key.
- **EmbeddedWallet → `ephemeral: true`** (`aztec.ts:222-228`); verify it covers the wallet-DB too (else explicit in-memory `walletDb.store`); demote `clearIndexedDB` to one-time legacy cleanup; fix BOTH stale comments (`aztec.ts:153`, `:238`).
- **Make playground source typecheckable:** fix `packages/playground/tsconfig.json` (bun-test types, `.css` declaration — smallest honest change), add a `typecheck` script, wire into root `test:typecheck` so CI inherits it. Pre-existing errors surfaced get fixed here and logged.
- Lockfile via **one local, command-scoped** `bun install --minimum-release-age=0` (5.0.0 is <1 day old vs the 7-day `bunfig.toml` gate). Never `bunfig.toml`, never CI.
- **Lock-diff scrutiny (concrete, per fable S3):** diff `bun.lock`; only `@aztec/*` versions may change; for each new/changed non-`@aztec` entry run `npm view <pkg>@<ver> time --json` and flag anything published after **2026-07-06**. **CI-parity:** `bun install --frozen-lockfile` exits 0.
- Fix any additional tsc-surfaced breaks (log in lessons; `packages/sdk/MIGRATION.md` entry only if the SDK's own API is forced to change — not expected).

**Validation gate:** override-install then frozen-install both exit 0; zero skips + all pins `5.0.0`; lock-diff clean (with the `npm view time` check on any stranger); `bun run test` exit 0 (lint + typecheck now incl. playground src + unit); `bun run --cwd packages/playground build` exit 0; `bun run --cwd packages/playground typecheck:scripts` exit 0; `bun run --cwd packages/playground test:e2e` (Playwright mocked project — 8 tests; the repo's "28 Playwright tests" counts all projects) green; `CRS_CACHE_VERSION` reads `5.0.0`; signing-key grep clean. Layers: lint · typecheck (sdk + playground src + scripts) · unit · UI-mock e2e. **RESULT: all green (see lessons/phase-1.md — includes the kv-store/Vite dev-server fix that P3b's smoke depends on).**

### P2 ✓ — Promotion workflow + publish hardening + land the PR — gate passed 2026-07-13 (PR #376 merged, all CI green incl. native-bb e2e; local download_and_verify_bb PASSED; +follow-up PR #378 for the 5.0.0 GET-/status-405 node-check fix found by P3b)

- **New `promote-latest.yml`** (workflow_dispatch; input `version`): sanity-checks the version is a published `@alejoamiras/aztec-accelerator` version matching the allowlist **`X.Y.Z` OR `X.Y.Z-revision.N`** (final-codex: stable-only contradicted the plan's own fix-forward recovery format), then `npm dist-tag add …@<version> latest` with the existing `NPM_TOKEN`. Inputs reach the shell **via `env:` indirection** (no `${{ }}` interpolation into `run:` — injection hygiene). Tag-only — never publishes. Rejected alternative: a `latest` input on `publish-testnet.yml` — can't promote-after-smoke without minting `5.0.0-revision.1`.
- **Close the promotion bypass — BOTH inputs (final-codex, two rounds):** `_publish-sdk.yml` is directly dispatchable, and both its `latest` boolean AND its free-text `dist_tag` (fed straight into `npm publish --tag`, `:101`, via `${{ }}` shell interpolation) could set `latest` with no smoke gate. Fix: **remove `latest` from the `workflow_dispatch` inputs block** (keep on `workflow_call` — `publish-testnet.yml` pipes it); make the dispatch `dist_tag` a **strict `type: choice`** limited to `[testnet, nightlies]`; route inputs into `run:` **via `env:` indirection** (no expression-to-shell injection); and add a guard step that hard-fails if the effective `dist_tag` is `latest` or unrecognized — covering the `workflow_call` path too (our callers hardcode `testnet`/`nightlies`; promotion happens ONLY via `promote-latest.yml`). Also give all npm-mutating workflows **one shared, non-cancelling concurrency group** (`publish-npm`) so a promote can't interleave with a publish.
- **Publish-workflow hardening** (all in the file P2 already touches): (i) `concurrency.cancel-in-progress: false` (canceling a half-done npm publish strands irreversible state; queue instead); (ii) **`build_accelerator: true` on its `_e2e.yml` call** (fable S2/P-5: today the publish-time gate exercises WASM-only — `_e2e.yml` defaults `build_accelerator: false` — so the native-bb proof would live only in the PR run; rc.1 flagged this exact gap); (iii) comment documenting the partial-failure rule: publish-step failed AFTER npm publish → never blind-redispatch; check `npm view` first; a redispatch mints `5.0.0-revision.1` by design; (iv) refresh the stale "while @aztec deps are rc-labeled" comment.
- Branch → PR → CI green → auto-merge (`main` branch-protected; unsigned commits via `git -c commit.gpgsign=false`).
- **Native-bb-5.0.0 e2e** (`sdk.yml`, `build_accelerator: true`; `setup-aztec` auto-detects the sandbox version from the sdk pin — fable-verified, so this genuinely runs a 5.0.0 sandbox): asserts `transmit`, not `fallback` → proves the prove/msgpack/proof interface against bb 5.0.0.
- **Runtime-download gate** (production's actual path — deployed 1.0.6 bundles bb 4.3.1, downloads 5.0.0 at runtime): `ACCELERATOR_DOWNLOAD_TEST=1 AZTEC_BB_VERSION=5.0.0 cargo test download_and_verify_bb -- --nocapture` from `packages/accelerator/core` (test name per fable; substring `download_and_verify` also matches). Current-arch proof; cross-platform rests on the diff-empty core crate + verified `barretenberg-*` matrix on the `v5.0.0` release. Either gate fails → **escalate: accelerator release; surface and hold.**

**Validation gate:** `bun run lint:actions` exit 0 (both workflow files); PR CI green — `sdk.yml` (native-bb `transmit` assert) + `app.yml` + `accelerator.yml` + `actionlint.yml`; local `download_and_verify_bb` passes; PR auto-merges. Layers: lint · typecheck · unit · **e2e (native bb 5.0.0) · download+digest**. *(Known flake: Playwright `install-deps` timeout — re-run.)*

### P3 — FPC redeploy → pre-publish smoke → publish `testnet` → acceptance smoke → promote `latest`

- **(a) Redeploy+fund the 5.0.0 SponsoredFPC.** **Fail-closed pre-flight** (run + eyeball before any signing): `node_getNodeInfo` → `nodeVersion 5.0.0` + `l1ChainId 11155111`; `node.getContract(<derived 5.0.0 salt=0 addr>)` logged explicitly, **RPC error ≠ absent** (error = stop). Already deployed → skip to funding. Else run `bun run packages/playground/scripts/deploy-sponsored-fpc.ts --salt 0x0` (**flag mandatory** — script defaults to `Fr.random()`) with **an explicit `BRIDGE_AMOUNT`** (final-codex: never rely on defaults; amount blessed via Ask 2). **Key intake: the gitignored `scripts/.env` ONLY** (`.gitignore:6` covers it — fable-verified) — not inline on the command line, which lands in shell history (final-codex); never logged, committed, or shell-history'd, and **delete `scripts/.env` when P3 completes** (final-codex LOW: ".env intake persists by design during the phase — the rule is scoped deletion, not never-touch-disk"). Sepolia wallet needs **ETH for gas** (the script mints its own test FJ — so the "drainable public FPC" exposure is economically ≈ the gas spent, not the FJ figure; the bridged amount is still set consciously as a norm). **Funding post-flight (final-codex — the draft claimed a balance printout that doesn't exist; the script prints only salt+address, `deploy-sponsored-fpc.ts:235-240`):** explicitly query the FPC's Fee Juice balance after the claim (aztec.js `FeeJuiceContract` balance read against the node) and record it in lessons; top up via `batch-fund-fpc.ts --salt 0x0 --amount <explicit>` (its default is 1M FJ and it bridges its entire L1 balance — always pass `--amount`).
- **(b) Pre-publish smoke (fable condition 2 — gates the publish).** `bun run --cwd packages/playground dev:testnet` from merged main: manual browser smoke against v5 testnet + the freshly redeployed FPC — account deploy + **token transfer** (WASM path minimum). This is the only pre-publish layer exercising tagging/discovery (incl. the handshake-default change) and the ephemeral-store init/sync time. Failure → fix before anything publishes.
- **(c) Publish + deploy (`testnet` only).** Dispatch `publish-testnet.yml --ref main` **immediately after the bump PR merge, nothing else queued** (artifact = audited SHA; record the merge SHA in lessons). Publishes the 5.0.0-derived version on `testnet` with `--provenance`; deploys playground → S3 → CloudFront. Workflow now queue-safe with a native-bb e2e gate (P2).
- **(d) Acceptance smoke — mandatory before any `latest` move.** `npm view` → `testnet` = 5.0.0-derived, `latest` untouched; live bundle bakes `5.0.0`; **registry-artifact install test** (final-codex: the publish step mutates package.json/exports at publish time, `_publish-sdk.yml:84-99` — a surface no workspace build exercises; fresh temp dir → `npm i @alejoamiras/aztec-accelerator@<version>` → import the public API and construct `AcceleratorProver`); **released-1.0.6 desktop app smoke** (genuinely downloads bb 5.0.0 at runtime — the only end-to-end test of production's download path) + **one Safari pass** (fable P-7: COEP `credentialless` support history) + a WASM pass.
- **(e) Promote `latest`** (joint step per Ask 1; timing per Ask 3): dispatch `promote-latest.yml` with the exact version verified in (d). **Rollback honesty (fable S1):** the only prior `latest` is 4.3.1 — a v4-line SDK already incompatible with the v5 network — so rolling back restores a *differently broken* default; real recovery is **fix-forward** to `5.0.0-revision.N`, which is a semver prerelease that `^5.0.0` ranges will NOT resolve (only bare/`latest` installers get it). A tag move also repairs nothing already installed. The flip is still low-blast-radius precisely because today's `latest` is already stale/broken.

**Validation gate:** pre-flight log (nodeVersion/chainId/getContract explicit); FPC deployed (getContract defined) + funded (claim receipt + **explicit post-claim balance query recorded in lessons** + smoke paying via it); **pre-publish transfer smoke passes BEFORE the (c) dispatch**; `publish-sdk` + `deploy-app` green; `npm view` `testnet` correct; live bundle `5.0.0` + HTTP 200; **registry-artifact install+import test passes**; released-1.0.6 native smoke + Safari + WASM passes; after (e): `latest` = same version. Layers: e2e-live-network (manual acceptance, two stages) + registry-artifact.

---

## Security & Adversarial Considerations

- **Supply chain (headline):** installing `@aztec/*@5.0.0` hours old, inside the 7-day gate. Mitigations: local-only command-scoped override (never `bunfig.toml`/CI; `bun.lock` embeds no age metadata; CI frozen throughout — `sdk.yml:57,76,95`, `_publish-sdk.yml:60`, `publish-testnet.yml:71`, fable-verified); whole-resolution lift compensated by the concrete lock-diff + `npm view time` check; exact pins + committed lock; `--provenance` authenticates **our** publish only — it does NOT validate the hours-old upstream (codex nuance). Residual upstream-compromise risk is what Ask 3's soak option buys — and only for unpinned-`latest` installers.
- **`latest` blast radius + rollback (rewritten per fable S1):** promotion is smoke-gated, tag-only via `promote-latest.yml` (stable-semver + published-version sanity check), and timed per Ask 3. Recovery story is honestly fix-forward (see P3e) — there is no good prior 5.x to roll back to, `-revision.N` won't reach `^5.0.0` ranges, and tags don't repair installed copies.
- **Publish non-atomicity:** `cancel-in-progress: false` + documented partial-failure rule (P2).
- **Publish-artifact provenance vs audit provenance (fable S2):** the dispatch runs at main-HEAD, not a pinned SHA → mitigated by dispatch-immediately-post-merge + recording the SHA + `build_accelerator: true` on the publish e2e so even an intervening merge gets the native-bb gate.
- **Promotion path least-privilege:** the one real bypass — `_publish-sdk.yml`'s direct dispatch could flip `latest` ungated — is **closed in P2** (dispatch inputs lose `latest`; call-only capability remains for `publish-testnet.yml`); all npm-mutating workflows share one non-cancelling concurrency group. Remaining standing gap: static `NPM_TOKEN` (no OIDC trusted publisher, no protected environments, mutable action tags) — not fixed here (ledger: repo-wide CI hardening pass; no NEW privilege added; Ask-1's hold is a process control for the agent, and with the dispatch bypass closed, the only ungated `latest` path left requires the repo owner's own credentials). `_publish-sdk.yml:129-136` keeps GitHub releases `--latest=false` unconditionally → the npm flip can't steal the desktop app's GitHub "Latest" badge (fable-verified).
- **L1 key handling (P3a):** disposable Sepolia wallet; **gitignored `scripts/.env` intake only** (inline env lands in shell history — final-codex); never printed (script prints only the derived address — fable-verified); fail-closed manual pre-flight compensates the script's fail-open `getContract` catch (in-script hardening = filed follow-up). **FPC drainability:** funded public sponsor is drainable by design; the FJ is freely-mintable testnet token, so economic exposure ≈ Sepolia gas — the explicit `BRIDGE_AMOUNT`/`--amount` discipline is hygiene + a norm, not a financial control.
- **Protocol/version:** testnet already on 5.0.0; prompt tracking reduces drift. Canonical salt=0 FPC front-run = a donation (public bytecode, deterministic address; both the pre-flight and the script's own already-deployed check handle it).
- **bb prove-interface trust:** gated by P2 native e2e + download test + P3d released-app smoke. Break → escalate; surface and hold.
- No new trust boundary → no `/harden` pass scheduled (consistent with rc.1/rc.2; user confirms at gate).

## Assumptions

**Facts** (all independently re-verified by fable; point-in-time observations captured as command output in lessons):
- `@aztec/*@5.0.0` npm 2026-07-13T09:39:46Z; upstream `latest`→5.0.0; our dist-tags today `latest=4.3.1`, `testnet=5.0.0-rc.2`; `@alejoamiras/aztec-accelerator@5.0.0` unpublished → clean publish version. All 13 distinct `@aztec` deps exist at 5.0.0 (zero-skip expected).
- v5 testnet node: `nodeVersion 5.0.0`, `rollupVersion 1821665230`, `l1ChainId 11155111`.
- `createSchnorrAccount(secret: Fr, salt: Fr, signingKey: Fq, alias?)` — required third arg in 5.0.0 (vs optional in installed rc.2). **Five** 2-arg call sites (listed in the table); `Fq` exported from `@aztec/aztec.js/fields`.
- `EmbeddedWalletOptions.ephemeral?: boolean` exists in 5.0.0 ("Use ephemeral (in-memory) stores"); playground currently persists and its recovery clears IndexedDB only (`aztec.ts:129-145`, `:267`); stale comments at `:153` + `:238`.
- Playground source has no working typecheck path (root `test:typecheck` = SDK only; `vite build` doesn't check; raw tsc fails on config gaps — reproduced).
- SponsoredFPC artifact sha `57b124ba… → 648b856a…` (recomputed twice, independently) → salt=0 address moved.
- **Entire `packages/accelerator/core/` crate diff-empty `accelerator-v1.0.6..HEAD`** (fable; supersedes the draft's file-scoped diff).
- `publish-testnet.yml`: `latest: false` hardcoded (`:52`), stale rc comment (`:46-47`), `cancel-in-progress: true` today, e2e call lacks `build_accelerator` → WASM-only publish gate (`_e2e.yml` default false). `_publish-sdk.yml` implements the `latest` dist-tag step (`:103-108`) and pins GitHub releases `--latest=false` (`:129-136`). All publish/bump workflows are dispatch-only → P3 sequencing can't be raced (fable).
- `update-aztec-version.ts` accepts `5.0.0`; auto-CRS + auto-Windows-checksum; silently skips unpublished packages (`:55`, `:116-119`) → zero-skip gate.
- `deploy-sponsored-fpc.ts`: env-only key intake (`:47-56`), prints only the derived address (`:81`) and, at DONE, **salt+address only — no balance printout** (`:235-240`, final-codex corrected the draft's claim); supports explicit `BRIDGE_AMOUNT` env (`:49`); header suggests a gitignored `scripts/.env` (`.gitignore:6` covers it); mints test FJ itself → wallet needs gas ETH; `getContract` catch is fail-open (`:197-207`); random default salt (`:43`). `batch-fund-fpc.ts`: `--amount` defaults to 1,000,000 FJ and it bridges its **entire** L1 balance (`:47`, `:199-200`) → always pass `--amount`.
- `_publish-sdk.yml` has BOTH `workflow_call` and `workflow_dispatch` triggers, each defining `latest` — the dispatch copy is the ungated-promotion bypass P2 closes.
- `setup-aztec` auto-detects the sandbox version from the sdk pin → P2's e2e runs a genuine 5.0.0 sandbox. The download test's real name is `download_and_verify_bb` (`downloader.rs:584`).
- COOP `same-origin` / COEP `credentialless` live + dev. Gate commands all exist as cited. min-age precedent: rc.1 + rc.2 local-only override; frozen installs don't re-apply the age check.

**Inferences** (attack these):
- `tsc` compiles clean against 5.0.0 after the 5 migrations + typecheck-enablement (grep can't see type-level breaks in e.g. `BBLazyPrivateKernelProver` internals). *P1 confirms; extras logged.*
- bb 5.0.0 prove + download interfaces compatible with deployed 1.0.6. *P2 e2e + download test + P3d released-app smoke; contingency = accelerator release.*
- The 5.0.0 salt=0 SponsoredFPC is not already on v5 testnet. *P3a pre-flight confirms either way.*
- `ephemeral: true` init/sync cost on testnet acceptable (fresh-random accounts; sync window starts at registration) AND it covers the wallet-DB store (upstream doc ambiguity — fable F3). *P1 verifies the option's coverage; P3b smoke times it; fallback = persistent + OPFS-aware recovery (follow-up-sized).*
- The exact OPFS lock/second-tab mechanics (PXE store vs wallet-DB) — plausible per changelog, unverified in code. *Downgraded from Fact per fable; moot under ephemeral; P3b/P3d spot-check.*
- Live rc.2 playground may still function against the 5.0.0 node (compat adaptors). *Non-load-bearing.*
- `FeeJuicePaymentMethodWithClaim`/`L1FeeJuicePortalManager` TS surface unchanged for our scripts. *`typecheck:scripts` + the real P3a run.*

**Asks — RESOLVED at the approval gate (2026-07-13):**
1. **Joint-stable rule → FULLY AUTONOMOUS** (user): both P3 dispatches (publish + `latest` promotion) are authorized by this approval once their gates pass — an explicit per-plan override of the joint-stable default. Remaining holds: empty Sepolia wallet, any gate failure, or the accelerator-interface escalation.
2. **Sepolia wallet → funded; `BRIDGE_AMOUNT` = 1000 FJ** (rc.2 parity — rc.2 lessons: bridged 1000 FJ, smoke txFee ~0.94 FJ).
3. **`latest` timing → same-day, smoke-gated** (codex-hybrid option; fable's defer-to-07-20 declined by user).

## Decision ledger

- **Chosen: A′** (track-now; FPC → pre-publish smoke → publish `testnet` → acceptance smoke → promote per Ask 3). **Rejected: draft-A** (promote-in-same-dispatch — can't smoke-gate `latest` without minting `5.0.0-revision.1`). **Rejected: B-as-drafted** (hold everything a week — dominated once promotion-timing was split out; its soak survives as Ask 3(ii)). Draft-B's "override user-visible in the artifact chain" rationale was corrected (both auditors: the override never touches published artifacts).
- **Codex round-1 (`reject`) dispositions:** (1) 5 call sites → **adopted**. (2) OPFS persistence/recovery → **adopted** via `ephemeral: true` (stronger than either codex option). (3) playground source typecheck → **adopted**. (4) split publish/promotion → **adopted** (`promote-latest.yml`). (5) cancel-in-progress + partial-publish rule → **adopted**. (6) released-1.0.6 smoke mandatory pre-`latest` → **adopted**. (7) FPC fail-closed → **partially adopted** (manual fail-closed pre-flight + human-in-loop; in-script hardening **rejected for scope**, filed follow-up). Plus: zero-skip gate, Sepolia-gas wording, provenance nuance, merge-SHA evidence — adopted. Protected environments / OIDC / action pinning → **rejected for scope** (standing repo-wide hardening; no new privilege added here).
- **Fable (`conditional approve`) dispositions:** C1 (5 sites + `Fq` imports) → **adopted**. C2 (pre-publish transfer smoke between P3a and the publish) → **adopted** (new P3b). C3 (OPFS recovery work item) → **adopted-stronger** (`ephemeral: true` + both comments + coverage verification). C4 (honest rollback story) → **adopted** (P3e + Security rewritten). C5 (explicit `latest`-timing Ask with both framings) → **adopted** (Ask 3). Non-blocking: pin-grep gate, `build_accelerator: true` on publish e2e, concrete `npm view time` scrutiny, Safari smoke, `download_and_verify_bb` naming, OPFS Fact→Inference downgrade — **all adopted**.
- **Final fresh-context codex pass (`reject` → all 4 findings folded):** (1) ungated `latest` bypass via `_publish-sdk.yml` direct dispatch → **adopted surgically** (dispatch loses the `latest` input; call-only keeps it; shared non-cancelling `publish-npm` concurrency group) — its protected-environment demand stays **rejected for scope** (documented, with the bypass now closed the residual requires owner credentials). (2) registry artifact never tested → **adopted** (P3d fresh-install + import test; a round-1 sub-point the fold had dropped). (3) funding proof didn't exist as written → **adopted** (draft's false "script prints balance" corrected; explicit `BRIDGE_AMOUNT`, post-claim balance query, `--amount` mandatory, `.env`-only key intake) — its "owner-approved min/max exposure" framing **rejected as overstated** (freely-mintable test FJ; exposure ≈ gas) but the explicit-amount discipline kept. (4) stable-only promote check vs `-revision.N` recovery contradiction → **adopted** (allowlist covers both; env-indirection for inputs). **Re-verdict round:** `conditional approve` — residual `dist_tag=latest` dispatch bypass → **adopted** (choice-typed input `[testnet, nightlies]`, env indirection, reject-`latest` guard on both paths); `.env` persistence rewording + delete-after-P3 → **adopted**; codex accepted the testnet-FJ exposure pushback.
- **Disputed/unresolved:** Ask 3's default (codex-hybrid same-day vs fable defer-to-07-20) — deliberately left to the user; both framings presented.
- **Out-of-scope landmines:** ~~Windows bb checksum~~ auto-pinned by the #372 tool (verify in P1 diff). Static `NPM_TOKEN` (now ×2 workflows) → future OIDC hardening. `deploy-sponsored-fpc.ts` fail-open `getContract` → follow-up. `-revision.N`-unreachable-by-ranges (fix-forward limitation) → inherent to the version scheme, documented.

## Post-implementation hardening

None scheduled (no new trust boundary; consistent with rc.1/rc.2). Static-token/protected-environment gap filed as a future hardening item, deliberately not this plan. User confirms at gate.

## Codex audit (mid, xhigh) — round 1: `reject` → all blockers folded

Session `019f5cda-c6d3-7b20-bd79-787824c6fd8e`. 7 blocking findings — every one reproduced against the repo and adopted or scope-rejected per the ledger. Full transcript: `audit-codex.md`.

## Fable audit (mid, Fable 5) — `conditional approve`, all 5 conditions folded

Independent re-verification of every Fact (npm, CDN tarballs, live RPC, live headers, git tags — several claims STRENGTHENED: whole-core-crate diff-empty; setup-aztec auto-detection; dispatch-only workflows unraceable). Conditions: (1) 5-site census + `Fq` imports, (2) pre-publish transfer smoke, (3) OPFS recovery item, (4) honest rollback story, (5) explicit `latest`-timing Ask — all folded; all non-blocking recommendations also adopted. Full transcript: `audit-fable.md`.

## Final fresh-context codex pass (mid, xhigh) — `conditional approve`, condition folded

Session `019f5ceb-34bc-7a31-9f97-3f992900974f` (fresh context; saw the consolidated plan + both audit transcripts + ledger). Round 1: `reject` with 4 findings — all verified (the balance-printout claim was indeed false; the `_publish-sdk.yml` dispatch bypass and the stable-only/`-revision.N` contradiction were real consolidation misses) and folded. Resumed with the deltas: **`conditional approve` (condition: prevent direct dispatch from setting or injecting `dist_tag=latest` via a strict non-latest allowlist and env indirection)** — folded into P2 (choice-typed `dist_tag`, env indirection, reject-`latest` guard on both trigger paths). Codex accepted the testnet-FJ funding pushback; its LOW `.env` rewording (scoped persistence + delete-after-P3) also folded. Transcript: `audit-codex.md`.

## Seeds (FINAL — approved scope: autonomous dispatches, BRIDGE_AMOUNT=1000 FJ, same-day latest)

**Recommended `/loop 15m`** (P3 has non-transcript-observable steps: dispatches, manual smokes, funding hold):

```
/loop 15m Drive implementations-plan/aztec-5.0.0-stable-2026-07-13 to completion (P1–P3); never idle. Each firing: read plan.md + lessons/; git status + log; PR open → gh pr view --json statusCheckRollup (no --watch); CI in-flight → gh run watch up to 10m (re-run the Playwright install-deps flake). No task? take the next plan.md step; after each meaningful edit run bun run lint + the touched package's tests. P1: aztec:update 5.0.0 (ZERO skips + grep all pins 5.0.0) → migrate all FIVE createSchnorrAccount sites + Fq imports (aztec.ts:473,:649, e2e-helpers.ts:25, deploy-sponsored-fpc.ts:149, batch-fund-fpc.ts:250) → EmbeddedWallet ephemeral:true (verify wallet-DB coverage) + fix comments aztec.ts:153+:238 → playground typecheck enablement (tsconfig + script + root wiring) → ONE local `bun install --minimum-release-age=0` (never bunfig/CI) → lock-diff scrutiny (npm view <pkg>@<ver> time for strangers, flag post-2026-07-06) → frozen-install proof → full P1 gate. P2: promote-latest.yml (tag-only; allowlist X.Y.Z OR X.Y.Z-revision.N; inputs via env: indirection) + _publish-sdk.yml dispatch hardening (remove `latest` from dispatch inputs; dist_tag → type:choice [testnet,nightlies]; env indirection; reject-latest guard on BOTH trigger paths) + shared non-cancelling `publish-npm` concurrency group across npm-mutating workflows + publish-testnet.yml hardening (cancel-in-progress:false, build_accelerator:true on its e2e, partial-publish rule comment, stale-comment refresh) → lint:actions → PR → CI (sdk.yml transmit assert) → local ACCELERATOR_DOWNLOAD_TEST=1 AZTEC_BB_VERSION=5.0.0 cargo test download_and_verify_bb (from packages/accelerator/core) → auto-merge. P3: (a) fail-closed pre-flight (nodeVersion 5.0.0, chainId 11155111, getContract logged, RPC-error ≠ absent) → Sepolia wallet needs gas ETH (empty → SURFACE AND HOLD) → key via gitignored scripts/.env ONLY (never inline — shell history; DELETE scripts/.env at P3 completion) → deploy-sponsored-fpc.ts --salt 0x0 (MANDATORY flag) with BRIDGE_AMOUNT=1000 FJ (rc.2 parity, APPROVED) → post-claim: query the FPC FeeJuice balance explicitly + record in lessons; top-up via batch-fund-fpc.ts --salt 0x0 --amount <explicit> if needed; (b) PRE-PUBLISH SMOKE: dev:testnet manual deploy+transfer vs the new FPC (WASM min) — MUST PASS BEFORE ANY PUBLISH — report result; (c) dispatch publish-testnet.yml --ref main immediately post-merge, nothing queued, record merge SHA (AUTONOMOUS — approved Ask 1); (d) verify npm testnet tag + live bundle 5.0.0 → registry-artifact test: fresh temp dir, npm i @alejoamiras/aztec-accelerator@<version>, import public API + construct AcceleratorProver → acceptance smoke: released-1.0.6 native path (REQUIRED) + Safari pass + WASM — report; (e) once (d) fully green, SAME DAY, dispatch promote-latest.yml with the exact verified version (AUTONOMOUS — approved Asks 1+3). Stuck or trade-off? /codex xhigh, log the consult in lessons. Phase green = its plan.md gate passes → mark ✓ in plan.md, file lessons/phase-N.md, print LESSONS_FILE=…, advance. 5× same-step fail → stop, reassess with codex. All ✓ → /code-review max --fix → commit fixes separately → codex post-impl audit (net diff + code-review summary + adversarial ask) → address high/critical → wrap-up report → stop. HARD LIMITS: never push main; never print the L1 key (scripts/.env only, deleted at P3 end); never expand scope; the ONLY holds = empty Sepolia wallet, a failed gate, or the accelerator-interface escalation.
```

**Alternative `/goal`:**

```
/goal All P1–P3 phases marked ✓ in implementations-plan/aztec-5.0.0-stable-2026-07-13/plan.md (per-phase headers in the file), each ✓ backed by its validation gate reported passing in the transcript; LESSONS_FILE=implementations-plan/aztec-5.0.0-stable-2026-07-13/lessons/phase-N.md printed per phase; pre-publish transfer smoke reported passing BEFORE the publish dispatch; npm dist-tags show testnet = latest = the 5.0.0-derived version in the transcript (both dispatches autonomous per approved Asks 1+3, same-day smoke-gated); 5.0.0 salt=0 FPC getContract defined on v5 testnet with post-claim balance recorded; registry-artifact install+import test passing; released-1.0.6 native smoke + Safari + WASM reported passing; /code-review max --fix applied+committed; codex post-impl audit high/critical addressed; bun run test and bun run lint:actions exit 0 in the transcript. Constraints: never push main; never print the L1 key (scripts/.env only, deleted at P3 end); BRIDGE_AMOUNT=1000 FJ; holds only for empty Sepolia wallet, failed gates, or accelerator-interface escalation.
```
