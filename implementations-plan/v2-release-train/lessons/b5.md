# B5 — real uninstall: lessons

## Cohort-task-0 (BLOCKING) — CONFIRMED: NSIS_HOOK_PREUNINSTALL exists, fires before exe delete

Resolved @tauri-apps/cli **2.10.1** (bun.lock). Fetched the authoritative template:
`tauri-apps/tauri@tauri-cli-v2.10.1:crates/tauri-bundler/src/bundle/windows/nsis/installer.nsi`.

All four hooks are `!ifmacrodef`-guarded `!insertmacro` sites:
- PREINSTALL / POSTINSTALL — Install Section.
- **PREUNINSTALL — Uninstall Section, invoked BEFORE `CheckIfAppIsRunning` + `Delete "$INSTDIR\${MAINBINARYNAME}.exe"`.** ⇒ the exe is ALIVE when PREUNINSTALL runs → `ExecWait '"$INSTDIR\AztecAccelerator.exe" --prepare-uninstall'` is viable as the primary.
- **POSTUNINSTALL — after all deletions + registry cleanup** ⇒ exe already gone → only NSIS-native inline cleanup (schtasks /Delete, reg delete) is reachable there; it is the BELT for when the exe invocation was skipped/failed.

Verdict: B5 F1 design is valid as written; NO owner-ratified-deviation needed. Proceed with
PREUNINSTALL(`--prepare-uninstall`) primary + POSTUNINSTALL native-inline belt.

## Design locked (post-research of hooks.nsi + main.rs --remove-ca-trust)

- **`--prepare-uninstall` CLI branch** (main.rs, right after the `--remove-ca-trust` branch @ :542):
  early-argv, no GUI, per-step status prints, non-zero exit if incomplete (scripted-caller contract),
  `return`. Calls a new `prepare_uninstall()` doing ownership-checked removal of autostart +
  crash-recovery + (if owned) trust/certs.
- **NSIS_HOOK_PREUNINSTALL** (NEW macro): SAME real-uninstall guard as POSTUNINSTALL (`$UpdateMode <> 1`
  AND canonicalized `$EXEDIR != $INSTDIR`) so an interactive UPGRADE never strips autostart/trust; inside
  the guard `ExecWait '"$INSTDIR\AztecAccelerator.exe" --prepare-uninstall'` (exe alive here). PRIMARY path.
- **NSIS_HOOK_POSTUNINSTALL** (EXTEND): keep the existing certutil/certs trust-belt; ADD the native-inline
  BELT — `reg delete` the Run value + `schtasks /Delete` the crash-recovery task — ownership-checked at the
  NSIS level (parse the Run value's quoted exe token, canonicalize, EXACT-equal `$INSTDIR\...exe`). Belt =
  fires when the exe invocation was skipped/failed (idempotent with the primary).
- **Guard duplication** PRE/POST is intentional (NSIS macros can't share locals cleanly); both compute the
  8.3-canonical $EXEDIR/$INSTDIR discriminator.
- Crash-recovery task must die on the PRIMARY path (recon collision #2: it relaunches the app 1 min after
  quit — a live task mid-uninstall re-spawns the app).

## Codex design review (pre-implementation) — 2 real bugs + polarity, all addressed

Consulted codex on the committed primary before building the belt (one-shot uninstaller). Verdict "do not
ship yet":
- **Real bug A**: POSTUNINSTALL's existing certutil/certs removal ran UNCONDITIONALLY on the real-uninstall
  path → it UNDID the primary's foreign-skip (wiped a copied install's shared CA). FIX: a native FOREIGN
  flag computed from the Run value gates ALL shared removal (certutil + certs + Run + task). Proven by the
  new `foreign Run value ⇒ genuine uninstall LEAVES the shared CA` wine case.
- **Real bug B**: `owned_reference_path().ok()` failed OPEN — no identity ⇒ `read_stored_target(None)` reads
  every healthy entry as ours. FIX: fail CLOSED (leave all) when identity can't be resolved.
- **Polarity FIX**: `Broken`/`Absent` don't PROVE ownership. Three states now:
  ConfirmedOurs (remove all) / NoEntry (remove shared, no entry) / ForeignOrUncertain (leave all). `Broken`
  (maybe a copy's removable-volume entry) + `Unreadable` moved to leave-shared.
- **Task belt = KEEP (codex picked (a))**: a scheduled task persists + fires every minute regardless of exe
  presence, so ownership-checked native `<Command>` deletion is load-bearing. Implemented via
  `AztecStrContains` matching the EXACT element `crash_recovery::task_xml` produces (escaped-domain match,
  deceptive-prefix-safe) + `nsExec` schtasks query. Codex's two-layer testing: the PARSER runs under wine
  with captured-XML fixtures (`parser.test.nsi`: ours/foreign/deceptive/escaped/truncated/empty), the live
  schtasks integration is Windows-CI-only.
- **Absent residual = KEEP + documented**: a second install that never armed autostart is invisible; it
  loses HTTPS state and must re-enable. A per-install registry is the complete future fix.
- **PREUNINSTALL invocation proof**: harness now bundles a stub `AztecAccelerator.exe` and asserts
  PREUNINSTALL actually runs `--prepare-uninstall` on real-uninstall / skips on upgrade.

Open for the code-review loop: whether the CLI must independently `<Command>`-check the crash-recovery task
(codex #4) vs gating on the autostart verdict (defensible — `set_enabled` bundles autostart+recovery; the
NSIS belt already does the independent check for the exe-gone path).

## Codex code-review round 1 — 5 findings, all fixed

The design review caught the ship-blockers; the CODE review caught the fiddly ones:
- **#1 Run-value presence**: `ReadRegStr $R0; $R0==""` conflates ABSENT with present-but-overlong/empty
  (ReadRegStr returns "" for both) → the latter (a foreign install's long path) read as absent and shared
  state got removed. FIX: establish presence via `EnumRegValue` over the Run key; a present-but-unreadable
  value now stays foreign.
- **#4 belt uniqueness/completeness**: a substring match for our `<Command>` element also fires on a
  MULTI-action task (up to 32 actions) that also contains ours, and on output TRUNCATED AFTER our element.
  FIX: require `</Task>` (complete) AND exactly one `<Command>` (`AztecStrCount`) AND our exact element.
  New fixtures `multi`, `truncated_after` ⇒ leave.
- **#6 lock + task-local ownership**: inferring crash-recovery ownership from the autostart verdict is
  cross-artifact inference on the very divergence/race path. FIX: hold `autostart.lock` across the whole
  transaction (`acquire_uninstall_lock` + lock-free `remove_entry_locked`, else the same-process re-lock
  deadlocks), and check the task TASK-LOCALLY — `crash_recovery::recovery_ownership` reads the Windows task
  `<Command>` (schtasks /XML, BOM-aware UTF-16) / Linux `ExecStart` (`systemd_exec_start`-serialized — a
  bare-path needle would NEVER have matched; caught before shipping). Only `Ours` disables.
- **#7a**: post-delete `/Query` treated `"error"`/`"timeout"` (nsExec couldn't run) as removed. FIX:
  distinguish `"0"`=present / error/timeout=unconfirmed / real-nonzero=removed.
- **#7b tests**: added a CARGO_BIN_EXE owned+idempotent e2e (runs the real binary) + a Windows real-task
  round-trip (`enable_crash_recovery`→`recovery_ownership`); CI runs them per-OS. `--lib`+`--tests` Windows
  cross-check (x86_64-pc-windows-gnu) clean — the recovery_ownership Windows path compiles.

## Codex code-review rounds 2–3 + scope arbitration

**Round 2 (5 findings incl. a MAJOR bug I introduced):**
- **#3 MAJOR**: a Foreign crash-recovery task left the task but `remove_trust_and_certs` still ran →
  stripped copy B's shared trust. FIX: a foreign recovery artifact is itself foreign evidence and gates
  ALL shared removal. Test: `foreign_recovery_unit_leaves_shared_trust`.
- #2 action-uniqueness (single `<Exec>`, no `<ComHandler>/<SendEmail>/<ShowMessage>`); #4 exit-code
  semantics; #3b Linux exactly-one-ExecStart; #1 bounded EnumRegValue loop. All fixed.

**Round 3 → scope arbitration (codex, ratified):**
- **#3 COM ITaskService — TRIM-OK.** Keep schtasks. `recovery_ownership` already right (code-1→Absent,
  other-nonzero/Err→Foreign). Lightweight consistency fix: `disable_impl` no longer treats EVERY nonzero
  /Query as gone — only code-1 confirms removal (was fail-open, pre-existing). Residual documented.
- **#5 RegQueryValueEx — TRIM-OK.** Keep `EnumRegValue` + bound; enumerating the current user's own short
  Run-value NAMES doesn't realistically fail. Residual documented.
- **#6 live Windows NSIS belt harness — REQUIRE, but a RELEASE/GA blocker, NOT a B5 merge blocker.** The
  belt's `nsExec::ExecToStack` UTF-16 capture + real schtasks deletion/preservation is the one thing wine
  fixtures + the Rust round-trip cannot prove (the Rust path decodes bytes itself; it does NOT share the
  nsExec wiring). MUST land as an immediate fast-follow before the first distributable: a small Windows-only
  harness — create our real task → belt → assert deleted; create a foreign-command same-name task → belt →
  assert preserved. Tracked as a release-gate item (see task); B5 merges without it.
