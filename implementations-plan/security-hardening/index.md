# Security-Hardening Ledger

Loop source-of-truth. Status: PENDING · IN-PROGRESS · DONE · BLOCKED. Pick the lowest-numbered PENDING whose deps are met. See `plan.md` for tiers, gates, and non-negotiable impl details.

| # | Cluster / branch | Findings | Tier | Deps | Status | PR | Notes |
|---|---|---|---|---|---|---|---|
| C0 | `sechard/ci-integration-gates` | (bootstrap) | light | — | DONE | #377 | gates now run on PRs into security-hardening; 3/4 dispatch green, actionlint dispatch failed on a PRE-EXISTING `shellcheck infra/*.sh` glob bug (no `.sh` in infra/; only bites on dispatch, skips on real PRs) → fold fix into C3 |
| C1 | `sechard/workflow-input-hardening` | F-006 | light | C0 | DONE | #385 | MERGED ec25b83. Validate-first (letter-led whole-string bash regex, LC_ALL=C) + env-route EVERY interpolation (dist_tag + version outputs) + `--tag=`. GATE 1 plan-audit REJECT→folded (arg-injection, validator multiline bypass, computed-output trust boundary); GATE 3 APPROVE-WITH-NITS→2 folded (no raw dist_tag in ::error::). clusters/C1-*, lessons/phase-C1.md |
| C2 | `sechard/core-request-safety` | F-003, F-009, F-011 | mid | C0 | DONE | #379 | MERGED e4e791b. perms-at-creation; permit-before-body+30s timeout + waiter-cap(429); reject trailing-dot. Post-impl audit R1 CHANGES→5 folded; R2 waiter-cap+dotted RESOLVED, #3/#5 folded, #2-Windows-DACL DEFERRED + crash-leftover ACCEPTED (lessons/phase-C2.md). Note: blueprint GATE waived for this retrofit cluster per user direction; C1+ blueprint-first |
| C3 | `sechard/action-pinning` | F-015 | mid | C0 | DONE | #386 | MERGED 4935c44. 122 remote uses SHA-pinned (incl GitHub-owned + aws-cred minter); rust-toolchain→master SHA + `toolchain: stable`; bun→1.3.14 (22 sites); node→24.18.0; actionlint→1.7.12 verified-download (no cache); shellcheck find/xargs; dependabot.yml. GATE 1 dual audit (codex+fable) folded; GATE 3 CHANGES-REQ (3 implicit-latest bun sites)→folded. clusters/C3-*, lessons/phase-C3.md |
| C4 | `sechard/updater-rollback` | F-004 | deep | C0 | DONE | #387 | MERGED 9e73d32. Layer A signed-manifest envelope + Layer B monotonic floor (pending high-water) + VerifiedUpdate newtype + capability denial + least-priv sign-update-feed job + PR-gate/live-feed verify. GATE 1 deep (3 plans+double audit). GATE 3 Codex REJECT (4H+3M+1L) FULLY FOLDED (H1 pending high-water, H2 commit-holds-lock, H3 version-matched tracker, M5 shared fail-closed gate, M6 feed-DoS doc, M7 live-feed verifier, L8 fsync, H4 sign smoke feeds). 164 core + 25 src-tauri tests, CI green. lessons/phase-C4.md |
| C5 | `sechard/infra-deploy-authz` | F-005 | deep | C0 | DONE | #388 | MERGED f0fa582. 3 per-pipeline OIDC roles bound via the `workflow` NAME claim (D1 resolved a Fable↔Codex contradiction on job_workflow_ref); release-feed isolated to exact `latest.json`; landing `--exclude releases` + IAM Deny (fixes latent feed-delete bug); publish-nightlies DELETED; main ruleset +linear/no-force/deletion/thread-res + required Actionlint check, 0 approvals; multipart lifecycle + S3 versioning; release-auth-preflight + --verify-tag + main-ref asserts. GATE 1 deep (3 plans + double audit → codex APPROVE); GATE 3 post-impl REJECT (4H+1M, IAM/S3/ruleset sound) → folded → re-audit APPROVE. Commit+validate only — **human applies** (clusters/C5-runbook.md: tofu apply + gh secret + ruleset PUT + smoke incl. negative cross-role AssumeRole). clusters/C5-* |
| C6 | `sechard/bb-cache-integrity` | F-007 | mid | C0 | DONE | #389 | MERGED 515bf39. Both download paths (download-bb.ts + downloader.rs) verify the GitHub asset digest + bounded-decompress (gzip-bomb cap) + private per-run `.{v}.tmp.<rand>` staging (0700) + tar-safety (symlink/hardlink/non-regular/extra reject) + `bb.sha256.json` marker (schema/ver/platform + archive+binary digests, post-codesign) + fail-closed delete-then-rename publish. Runtime `verify_cached_bb` rehash-on-use; `find_bb(Some(v))` hard-errors on ANY cache failure (NO sidecar/~/.bb/$PATH fallback); `resolve_version` bundled→None; in-use eviction exemption; stale-stage reap. GATE 1 mid (dual audit codex-reject+fable-conditional → 2 fresh final-codex passes r2/r3). GATE 3 post-impl codex REJECT→changes-requested (2 MEDIUM: unbounded TS decompression + incomplete negative gate) FULLY FOLDED (gunzipSync cap + nlink hardlink + streamed hash + 7 TS + 1 Rust fixture test + stale-reap). 36 root-scripts + 172 core tests, clippy/fmt/actionlint + full CI (31 checks) green. clusters/C6-*, lessons/phase-C6.md |
| C7 | `sechard/bb-windows-provenance` | F-008 | mid | C0, C6 | DONE | #390 | MERGED d383313. Removed the circular auto-pin (pinWindowsBbChecksum deleted — a twice-downloaded asset is not evidence). WINDOWS_BB_CHECKSUMS now structured `{sha256, provenance, note}`; resolveWindowsBbChecksum fail-closes on missing / non-manual-review (attestation reserved) / malformed-sha / empty-note; pins honestly labeled legacy change-detectors. `_aztec-update.yml` auto_merge→merge_mode:none|auto (default none leaves the PR OPEN; the `false` branch's IMMEDIATE `gh pr merge --squash` removed); job-level perms + create-PR token loses contents:write + separate contents-scoped merge token + close-not-delete stale; forced-input validated whole-string (multiline-safe) before $GITHUB_OUTPUT; jq --arg. DELETED aztec-nightlies.yml (unused ungated caller); post-install check-windows-bb-pin.ts. Empirical: no aztec attestation (404) → manual-review is a change-detector (SEC-02 residual, human-gated ruleset-bypass readback in C7-runbook). GATE 1 mid (codex+fable dual REJECT → revised → final codex confirms direction); GATE 3 post-impl codex changes-req (3M+2L) FOLDED. 10 copy-bb + 67 root-scripts tests, full CI (22 checks) green. clusters/C7-* |
| C8 | `sechard/desktop-platform-secrets` | F-010, F-016 | mid | C0 | DONE | #391 | MERGED 96dc125. F-010: `systemd_exec_start` serializes an absolute-UTF-8 exe path to a safe `":/path%%"` (reject-set per systemd `string_is_safe`: controls/newline/`\`/`"`/`'`/`*`/`?`/`[`/non-UTF-8; `:` prefix disables `$` expansion; `%`→`%%`); `enable_impl` fail-closes (removes stale unit); a `set_autostart` `autostart_path_is_safe` preflight refuses+disables an unsafe `current_exe` BEFORE the plugin (closes injection across systemd/.desktop/plist/Run-key), surfaced to UI; capability drops raw `autostart:allow-enable` (webview bypass); `disable_impl` removes-before-reload + confirmed-disarm bool. F-016: CA `KeyPair` in `Zeroizing` + early drop (honest residual: only rcgen's serialized DER, not the ring backend/swap/core-dump). GATE 1 mid→deep (codex+fable dual REJECT → revised → 2 final-codex; F-016 approved, F-010 sharpened); GATE 3 post-impl codex 3 LOW FOLDED (Windows-CI test, lockfile/schema, disable cleanup; enable()→Result rollback deferred as documented residual). 13 tests, clippy/fmt + full CI (21 checks) green. Per-platform auto-launch formatting = documented 3rd-party residual. clusters/C8-* |
| C9 | `sechard/authorize-popup-safety` | F-014 | light | C0 | DONE | #392 | MERGED 90f9573. Show the FULL canonical origin (selectable, keyboard-focusable, `dir=ltr`+`unicode-bidi:isolate`, punycode-canonical) — NO truncation/PSL (codex proved middle-ellipsis can't guarantee eTLD+1 visibility: private suffixes/ports/IPv6/long labels); `.popup-scroll` wrapper + fixed reachable Allow/Deny footer + `justify-content: safe center` (overflowing origin's START stays reachable). Remember default-UNCHECKED + "Allow once"/"Always allow" labels. GATE 1 light codex REJECT→folded (full-display design); GATE 3 post-impl codex changes-req (HIGH centered-overflow-hides-start + MED weak-test)→FOLDED (safe-center, restore shared-container centering, real 400×300 reachability test). Playwright-mock + WebDriver e2e updated; full CI (21 checks) green. DEFERRED (codex-ranked, tracked): focus-swap stacked-prompt click-steal (MED), extension-scheme host validation (LOW), get_pending_auth server-authoritative display binding (LOW). clusters/C9-* |
| C10 | `sechard/tauri-trust-boundary` | F-012 | deep | C0, C9 | IN-PROGRESS | — | GATE 1 CLOSED (deep: 3 legs + double audit codex+fable both CHANGES-REQ→folded → plan v2 → final codex APPROVE+1 MED folded). SOURCE-PROVEN: AppManifest flips app cmds to per-window default-deny (webview/mod.rs:1819). D7→DROP core:default (inert today); D8→built-debug Linux lane (is_dev()==!custom-protocol). Now GATE 2 (P0 deps). clusters/C10-*, lessons/phase-C10.md |
| C11 | `sechard/incumbent-identity` | F-002 | deep | C0, **F-001 contract** | BLOCKED | — | needs F-001 `InstallationIdentity`; do not build 2nd identity system |

**Sequencing:** C0 first (unblocks CI on PRs into security-hardening). C1–C10 then proceed (cut each branch AFTER the prior merges). C11 stays BLOCKED until F-001's team ships the identity contract.

**Human-gated closeout:** F-005 (`tofu apply` + ruleset API apply + secret cutover + read-back), the temporary `security-hardening` CI trigger/ruleset removal in the final `main` integration PR, and F-002 (F-001 dependency). See `plan.md` → "Human-gated closeout".

**Deferred follow-ups (tracked):**
- **F-003 Windows DACL** — apply an explicit owner-only Windows ACL to the prove workspace + an effective-ACL test (needs Windows CI). Deferred from C2's R2 audit: the confirmed F-003 vuln is resolved on all platforms by `0o700`/`0o600`-at-creation; this hardens against an out-of-threat-model attacker who already controls the per-user data dir. See lessons/phase-C2.md.

## Lessons
Per-cluster debugging logs land in `lessons/phase-<cluster>.md` (Codex consults logged there per the AFK protocol).
