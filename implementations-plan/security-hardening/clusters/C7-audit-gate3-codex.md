Scope note: `c7-gate3.diff` was absent, so I audited the complete contiguous C7 range `052c58d..HEAD`.

## Findings

1. **MEDIUM — The `none` path still receives a merge-capable `GITHUB_TOKEN`.**  
   The caller grants `contents:write` at workflow scope ([aztec-stable.yml:15](/home/homelab/Projects/aztec-accelerator/aztec-accelerator-2/.claude/worktrees/sechard-bb-windows-provenance/.github/workflows/aztec-stable.yml:15)), while `create-pr` has no job-level downgrade ([\_aztec-update.yml:161](/home/homelab/Projects/aztec-accelerator/aztec-accelerator-2/.claude/worktrees/sechard-bb-windows-provenance/.github/workflows/_aztec-update.yml:161)). Reusable workflows inherit the caller’s token permissions, and every action can access `github.token`; the checkout also uses that token ([\_aztec-update.yml:184](/home/homelab/Projects/aztec-accelerator/aztec-accelerator-2/.claude/worktrees/sechard-bb-windows-provenance/.github/workflows/_aztec-update.yml:184)). GitHub’s merge endpoint requires exactly `contents:write`. Therefore, the App-token split is correct, but the claim that `create-pr` has no merge/delete capability is false because of the residual `GITHUB_TOKEN`. Add job-level `permissions`: read-only for `check-update`/`create-pr`, write only for `update`. [GitHub reusable-workflow permissions](https://docs.github.com/en/actions/reference/workflows-and-actions/reusing-workflow-configurations), [merge endpoint permission](https://docs.github.com/en/rest/pulls/pulls#merge-a-pull-request).

2. **MEDIUM — Forced-version validation is multiline-bypassable before `$GITHUB_OUTPUT`.**  
   The validation is textually before output emission, but `grep` succeeds when *any line* matches ([\_aztec-update.yml:72](/home/homelab/Projects/aztec-accelerator/aztec-accelerator-2/.claude/worktrees/sechard-bb-windows-provenance/.github/workflows/_aztec-update.yml:72)). I reproduced acceptance of `5.0.0\nneeds_update=false`, which creates an injected output record. The rejected value is also echoed raw into workflow-command parsing ([\_aztec-update.yml:73](/home/homelab/Projects/aztec-accelerator/aztec-accelerator-2/.claude/worktrees/sechard-bb-windows-provenance/.github/workflows/_aztec-update.yml:73)). Downstream TypeScript revalidation ([update-aztec-version.ts:92](/home/homelab/Projects/aztec-accelerator/aztec-accelerator-2/.claude/worktrees/sechard-bb-windows-provenance/scripts/update-aztec-version.ts:92)) limits this from becoming current shell/URL execution, but the promised output trust boundary is not fixed. Use a Bash whole-string `[[ ... =~ ... ]]` check and a generic error, as already done in [\_publish-sdk.yml:40](/home/homelab/Projects/aztec-accelerator/aztec-accelerator-2/.claude/worktrees/sechard-bb-windows-provenance/.github/workflows/_publish-sdk.yml:40).

3. **MEDIUM — The production resolver accepts a malformed/manual-review pin with no review record.**  
   `note` is required by the schema ([copy-bb.ts:67](/home/homelab/Projects/aztec-accelerator/aztec-accelerator-2/.claude/worktrees/sechard-bb-windows-provenance/packages/accelerator/scripts/copy-bb.ts:67)), but the resolver validates only provenance and SHA ([copy-bb.ts:109](/home/homelab/Projects/aztec-accelerator/aztec-accelerator-2/.claude/worktrees/sechard-bb-windows-provenance/packages/accelerator/scripts/copy-bb.ts:109)). I confirmed `{sha256: valid, provenance:"manual-review", note:""}` is accepted. The aggregate test checks only `length > 0`, so whitespace passes too ([copy-bb.test.ts:57](/home/homelab/Projects/aztec-accelerator/aztec-accelerator-2/.claude/worktrees/sechard-bb-windows-provenance/packages/accelerator/scripts/copy-bb.test.ts:57)). Validate `typeof note === "string" && note.trim().length > 0` inside the resolver and add empty/whitespace/unknown-provenance tests.

4. **LOW — `branch_prefix` is injected into jq source code.**  
   It is interpolated inside the `--jq` program ([\_aztec-update.yml:198](/home/homelab/Projects/aztec-accelerator/aztec-accelerator-2/.claude/worktrees/sechard-bb-windows-provenance/.github/workflows/_aztec-update.yml:198)). A prefix such as `")) | .number #` selects every open PR on the target branch for closure. The only current caller hard-codes the prefix ([aztec-stable.yml:30](/home/homelab/Projects/aztec-accelerator/aztec-accelerator-2/.claude/worktrees/sechard-bb-windows-provenance/.github/workflows/aztec-stable.yml:30)), so there is no present external exploit path, but the reusable boundary is unsafe. Pass it to standalone `jq` via `--arg` or validate it.

5. **LOW — One active runbook retains a dangling nightly-workflow statement.**  
   The C5 operator runbook still says deleted `publish-nightlies.yml` is “inert” ([C5-runbook.md:107](/home/homelab/Projects/aztec-accelerator/aztec-accelerator-2/.claude/worktrees/sechard-bb-windows-provenance/implementations-plan/security-hardening/clusters/C5-runbook.md:107)). Historical audit/plan references are appropriately archival. This does not reopen F-008.

## Verified behavior

- There is no current automatic path that lands a new Windows bb version. `aztec-stable` uses `merge_mode:none`, and the only `gh pr merge` in `_aztec-update.yml` is conditional on `auto` and uses `--auto` ([\_aztec-update.yml:262](/home/homelab/Projects/aztec-accelerator/aztec-accelerator-2/.claude/worktrees/sechard-bb-windows-provenance/.github/workflows/_aztec-update.yml:262)). The separate merge in `release-accelerator.yml` is also `--auto` and only bumps the application’s own version.
- Missing pins, old string-shaped pins, non-`manual-review` provenance, and malformed SHA values fail closed. `fetchWindowsBb` correctly consumes the resolver’s returned string.
- The create-PR App token itself is properly repo-scoped and lacks `contents:write`; the conditional merge token has the required scopes. Dropping caller `pull-requests:write` is safe because every PR operation uses the App token.
- `dist_tag` is quoted data and currently fixed to `rc`; it presents no shell or fetch-URL injection. `branch_prefix` has the jq issue above, not shell execution.
- `check-windows-bb-pin.ts` performs only resolution/reads, uses `resolveAztecBb().version`, and never fetches or writes. Exit zero is correct: hard failure would prevent creation of the PR to which the human must add the pin.
- No remaining consumer assumes `WINDOWS_BB_CHECKSUMS` values are strings.
- Deleting `aztec-nightlies.yml` removes the sole automated ungated-nightlies caller. No current workflow calls `_aztec-update.yml` except stable.
- Legacy notes honestly identify the pins as non-independent change detectors, although runtime note enforcement needs fixing.
- Focused tests: 20 passed. `actionlint` and `git diff --check` passed. Actionlint does not detect the multiline/output or permission-separation semantics above.

VERDICT: changes-requested (inherited contents-write token; multiline forced-version validation/output injection; resolver accepts an empty review note)