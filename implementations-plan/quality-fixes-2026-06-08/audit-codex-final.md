conditional approve (conditions: normalize the stale F-01 callback decision to flat `AppState` everywhere; make `ALLOWED_ORIGINS`-present-but-empty stay “deny all browser origins” and test it; treat PR-2’s Windows crash-recovery E2E and PR-4’s SDK build as required gates, not recommendations)

- `## Assumptions` and `## Decision ledger` still contradict `§F-01` and `## Audit revisions`. `Assumptions` Ask 3 (`DesktopCallbacks`) and the ledger line “chose `DesktopCallbacks`” are stale, while the actual plan body reverted to flat `AppState`. That is not just editorial noise: `§F-03` still says `build_desktop_callbacks -> DesktopCallbacks`, so an implementer could reintroduce the wrapper the audits already rejected. Gate fix: make every section say “flat `AppState` only”; if a local helper struct remains in `src-tauri/main.rs`, label it as GUI-local and non-core.

- `§F-02`/`## Audit revisions` preserve trim/drop tolerance, but they do not explicitly preserve the current `ALLOWED_ORIGINS` presence semantics. Today, `packages/accelerator/server/src/main.rs:43-57` enables auth gating whenever the env var is present, even if splitting/trimming yields `[]`; that means `ALLOWED_ORIGINS=""` or whitespace-only currently denies browser callers rather than auto-approving them. A naive `parse_allowed_origins_env(...)->Ok(vec![])` integration could silently widen access by mapping empty output to `(None, None)`. Gate fix: state that “env present, even if empty after trimming, still instantiates auth/config” and add a headless regression test for `""`/whitespace-only/trailing-comma.

- One open Ask should be promoted from “recommendation” to an actual blocker: the Windows `AddrInUse` bow-out in `packages/accelerator/src-tauri/src/main.rs` is moving under F-03, and the only real coverage for that branch is `.github/workflows/_e2e-crash-recovery-windows.yml`, which is not on the normal PR gate. Since the whole claim for F-03 is “pure move,” that workflow needs to be explicitly required before PR-2 merges, not merely suggested in `## Assumptions`.

- PR-4 validation is still short of the published SDK surface. `sdk.yml` runs lint, `tsc --noEmit`, unit tests, and E2E, but it does not build `packages/sdk`; the publish workflow later rewrites `package.json` to ship `dist/index.js` and `dist/index.d.ts` from `_publish-sdk.yml`. Because F-05/F-06 touch the barrel/public surface, source-only typecheck is not enough to prove the npm artifact is intact. Gate fix: require `bun run --cwd packages/sdk build` (or an assertion against built `dist/index.d.ts`) in the plan’s PR-4 gate, not only as a low-priority note in `## Audit revisions`.

- `respond_auth` malformed-origin handling is still under-specified after typing `AuthorizationManager.pending`. In normal flow I do not see a remaining exploitable canonicalization bypass: the popup round-trips the canonical origin string produced by the server, and the `ws`/`wss` non-widening + punycode coverage are otherwise folded correctly. But the plan should stop leaving this branch as a floating Ask. Either fold in a concrete malformed-input behavior for `packages/accelerator/src-tauri/src/commands.rs:105-121` or explicitly accept timeout fallback; don’t leave the implementation to infer it at merge time.