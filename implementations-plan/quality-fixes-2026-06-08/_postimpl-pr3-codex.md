Post-implementation ADVERSARIAL review of PR-3 (F-07 + F-09 + F-08, all BEHAVIOR-PRESERVING). cwd = repo root. Diff: `git diff main...quality/pr3-local-refactors` (commits d729ee5 F-07, d50f9df F-09, 6c0bb5c F-08). Read the real source; verify the no-behavior-change claim + catch regressions.

**F-07** (`src-tauri/src/certs.rs`) — introduced `CertPaths { ca_cert, leaf_cert, leaf_key }` (live/staged/exists/swap_into/remove); `write_new_cert_set(&CertPaths)`; removed the 3 served-path accessors (kept `ca_key_path`). Verify: does `write_new_cert_set` write the same 3 files in the same order? `rotate()`'s `staged.swap_into(&CertPaths::live())` renames in the SAME order (ca→leaf→key)? `staged.remove()` on trust-failure removes the same 3 staged files? Does `certs_exist` keep the leaf-VALIDITY check (not just `.exists()`)? Do `install_ca_trust`/`is_ca_trusted` still target the live `ca.pem`? Any place the old accessors covered that's now wrong?

**F-09** (`src-tauri/src/{server,main,commands}.rs`) — `server::spawn_https(state, tls_config)` replacing two inlined `spawn(start_https)`+error-log blocks. Verify byte-identical wrapper + that each caller's divergent preamble is preserved UPSTREAM (main.rs `try_start_https` resets Safari Support on TLS-load failure; `enable_safari_support` propagates the error).

**F-08** (`core/src/server/prove.rs` + `server.rs`) — THE behavior-sensitive one. `resolve_version` is now pure sync (`ResolvedVersion { version, to_download }`, no status/download); `prove()` owns the status machine. Verify the emitted `on_status` sequence is EXACTLY preserved: no-download = `[Proving, Idle]`; download = `[Proving, Downloading, Proving, Idle]` (the redundant LEADING Proving must remain). Did the `download_bb` call + the `cleanup_old_versions` background spawn + the `download_failed` error move into `prove()` unchanged? Is the `bundled_owned` recompute in `prove()` the same value as before? Any borrow/lifetime/ordering drift? Does the bundled/uncached `to_download` decision match the old cache-check exactly?

Lead with a one-line verdict (`clean` / `issues: …`), then findings by severity with file:line. ~400–700 words.
