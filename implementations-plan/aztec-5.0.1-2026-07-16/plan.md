# Plan — Bump `@aztec/* 5.0.0 → 5.0.1` + standards-token migration + SDK 5.0.1 release (`/blueprint mid`)

**Tier:** `mid` (rubric: external coupling HIGH; novelty/blast/irreversibility/migration MODERATE — 4th bump cycle on hardened rails; the token swap is new demo surface). **Status: ✅ APPROVED 2026-07-16** (verdict: `approve`, unconditional; Phase-0 autonomy carried over — both dispatches autonomous, P4 holds only on auth). **All three audit rounds folded:** codex round-1 `reject` → adopted/dispositioned; fable `conditional approve` (5 conditions) → adopted; final fresh-context codex **`conditional approve`** (5 fold-repair conditions) → adopted.

## Goal

Ship the **5.0.1 client-security patch** (npm 2026-07-15T19:15Z): bump the 24 `@aztec/*` pins, **switch the playground's token demo** to **`@aztec-foundation/aztec-standards@5.0.1`** (in a **separately-revertible commit** — see the contingency), redeploy+fund the salt-0 SponsoredFPC (artifact `648b856a… → 15cf50ce…`, third consecutive move), publish SDK **5.0.1** on `testnet`, redeploy the playground, promote to `latest` same-day smoke-gated, extend the bump tool to keep the standards package in version-lockstep, and **deprecate `@alejoamiras/aztec-standards`** on npm. All 5.0.0-cycle machinery inherited (fable re-verified it on main: promote-latest sole-path, publish-time native gate, queue-not-cancel, 3-graph typecheck, production smoke, no-cache sqlite assets).

**Why now:** the testnet node is **still 5.0.0** (verified; operators optional) — no drift pressure. The driver is that 5.0.1 closes **attacker-triggerable client vulnerabilities live today** for our playground users and SDK consumers: undecryptable-note vector (#24665), permanent note-sync freeze (#24668), handshake forgery (#24403), out-of-scope secret access (#24616). **The security patch must never be held hostage by the demo swap — see the decoupling contingency.**

**Done =** SDK `5.0.1` on `testnet` + `latest`; playground live on 5.0.1 with the standards token demo proven on the deployed production bundle; 5.0.1 salt-0 FPC deployed+funded 1000 FJ; bump tool lockstep-aware; old standards package deprecated (all versions); CI green; no accelerator app release (v5.0.1 carries the full 17-asset barretenberg matrix, verified).

## Change assessment (changelog + migration → our surface)

| Item | Our exposure |
|---|---|
| aztec.nr breaking change (note-validation siloing #24644) | N/A — no Noir contracts in-repo (fable re-verified). Standards 5.0.1 compiled against it upstream. |
| **kv-store storage rework (#24631)** | **Pre-verified inert**: `@aztec/kv-store@5.0.1` keeps `dest/sqlite-opfs/{index,worker}.js`, `internal/ordered-binary-browser.js`, and the `#msgpackr`/`#ordered-binary` imports map byte-identical (fable re-verified against the exact `vite.config.ts:51-52,138-148` expectations); playground is `ephemeral: true` everywhere incl. the FPC script. Identical layout ≠ identical runtime (codex) — the P1 gates + P3 smokes are the behavioral proof. |
| **#24403 handshake-forgery domain separator** (fable P-5) | A cross-client-version tagging discontinuity in principle (5.0.0-client sender ↔ 5.0.1-client recipient may derive different handshake secrets). **Negligible by construction here**: the demo's Alice+Bob live in one ephemeral 5.0.1 PXE; no persisted client state exists (ephemeral since the 5.0.0 cycle); the only reused 5.0.0 on-chain state is the FPC (no private notes, being redeployed anyway). SDK consumers bring their own aztec.js. |
| OPFS handle-release (#24647), sync-wedge (#24655), security fixes (#24665/#24668/#24616), future-dated-message rejection (#24645) | Pure client-side benefit; no API surface. #24645 also tightens the hostile-input envelope our users face. |
| Explicitly N/A | #24479 (aztec.nr fee-payer phase assert — recompiled upstream into the 5.0.1 artifacts we consume; the FPC fee path is exercised by every smoke), #24636/#24662 (TXE/testing oracles), #24629 (aztec-kit in-process network), #24579 (prover-node ops), #24476 (validator CLI). |
| **Token-contract swap** | Playground `aztec.ts` (:16, :589, :686, :708, :726, :744-745): import `TokenContract` from `@aztec-foundation/aztec-standards/dist/src/artifacts/Token.js` (deep path — package ships no `exports`/`main`; **precedent-strengthened by fable**: the installed noir-contracts Token.js uses the byte-identical `with { type: 'json' }` + `@aztec/aztec.js/*` subpath pattern through this exact vite7+tsc pipeline, and `moduleResolution: "Bundler"` permits deep paths precisely because there is no exports map). Deploy via `deployWithOpts({ method: 'constructor_with_minter', wallet }, "Accelerator", "ACEL", 18, alice, AztecAddress.ZERO)` — **`auth_contract = ZERO` VERIFIED from the AztecProtocol/aztec-standards Noir source by the fable audit**: constructor docs say "zero address to disable", the hook is guarded by `if !auth.eq(AztecAddress::zero())`, no assertion constrains it; `transfer_private_to_private(alice, bob, 500n, 0)` (`#[authorize_once]`, nonce-0 self-call is the standard path); `mint_to_private(to, amount)` and `balance_of_private` unchanged shapes. NOTE (fable A2): the package declares **zero dependencies** but its generated JS has **undeclared runtime imports of `@aztec/aztec.js/{abi,contracts,fields}`** — resolved via hoisting against OUR pins; this mandates version-lockstep (hence the bump-tool extension) and both are 5.0.1 this cycle (`config.aztecVersion: "5.0.1"` in-package). |
| **SponsoredFPC artifact hash moved** (recomputed by both auditors) | Salt-0 redeploy+fund repeats. The 5.0.0 FPC's ~999 residual test-FJ is abandoned (free-mint token). |
| 5.0.1-client vs 5.0.0-node | Release framing verified verbatim ("optional for node operators"). P3b proves empirically before anything publishes. |

## Approach A′ — Single-shot WITH decoupling contingency (primary; fable P-1 hybrid)

One PR: bump + standards swap (swap as its own commit), inherited P3 pipeline, deprecation last. **Pre-authorized contingency, fully operationalized** (final-codex corrected the mechanics — P3b runs post-merge, so the revert is a PR, and the later gates must relax when it fires): if the P3b standards-token leg fails on live testnet and isn't quickly fixable —
1. **Revert PR**: `git revert` the swap commit → branch → PR → CI → auto-merge (the bump stays).
2. Re-run the P3b smoke as the **old-token flow** (the reverted bundle's actual demo).
3. Publish → acceptance → **promote 5.0.1 with the OLD-token live-bundle pass** (the standards-flow requirement in P3d applies only while the swap is on main — the security patch is never hostage).
4. Later: revert-the-revert PR → CI → merge → **playground-only** `publish-testnet.yml` dispatch with `skip_sdk_publish=true` (deploys the dispatch SHA — lever verified) → **standards-token production smoke on the deployed bundle** → only then P4 (deprecation strictly after the new package is proven live in the demo).

## Approach B — Two-cycle split (competing outline — REJECTED, both auditors' input folded)

Pure bump cycle first, token swap as a second playground-only cycle. Codex preferred a strengthened B (faster security shipping + an old-token compatibility matrix); fable showed B's real advantage is fully captured by the A′ contingency at a fraction of the cost, and the compatibility-matrix ask misfires on this architecture (ephemeral client state by design; no persisted 5.0.0 notes exist to interop with — disposition in the ledger). B costs a second full dispatch+smoke cycle for hours of earlier shipping.

---

## Phases

### P1 ✓ — Bump + standards integration + tool lockstep + lockfile — gate passed 2026-07-16

- `bun run aztec:update 5.0.1` — pins (24) + auto-CRS + auto-Windows-checksum (confirm fetched sha vs the GitHub v5.0.1 release asset digest). Zero-skip gate: tool reports none + `grep -rn '"@aztec/' packages/*/package.json` all `5.0.1`.
- **Extend `update-aztec-version.ts`** (fable P-4, in-cycle): bump `@aztec-foundation/aztec-standards` in lockstep with `@aztec/*` (explicit package allowlist, not a loose prefix match) + extend the tool's unit test + include the package in the zero-skip grep. **Make the scripts tests a REAL gate** (final-codex: nothing runs `scripts/*.test.ts` today — root `test:unit` covers only the three packages): add a root `test:scripts` (`bun test scripts/`) wired into root `test:unit`, and add the same step + a `scripts/**` path trigger to `sdk.yml` (release tooling rides the SDK pipeline) so CI enforces it.
- **Add `@aztec-foundation/aztec-standards@5.0.1`** (exact pin) to `packages/playground/package.json`. **Attestation verification at install time** (fable A1 + final-codex subject-binding): re-fetch the attestation independently (`registry.npmjs.org/-/npm/v1/attestations/…` — retrievable, verified: npm-publish v0.1 + SLSA v1), **compare the attestation's subject digest against the installed tarball's integrity** (lockfile `integrity` / `npm view dist.integrity` — they must be the same artifact, not two separately-recorded facts), skim the pinned `release.yml` at commit `c74541f7…` for anything anomalous, and record all of it (binding: `AztecProtocol/aztec-standards` `release.yml` @ `refs/tags/v5.0.1`, commit `c74541f7cf2bb23b704e96fd326ea95d98252669`, matching packument gitHead) in lessons. Mismatch anywhere = STOP.
- **Token-flow rewrite as its OWN commit** (revertability is the contingency's mechanism): import swap (deep path), `constructor_with_minter(…, alice, AztecAddress.ZERO)` (semantics already source-verified — cite fable's finding in the code comment sparingly), `transfer_private_to_private(alice, bob, 500n, 0)`, balances unchanged. Honest gate note: **no unit/mock layer executes token methods** — the mocked suite asserts UI states, the production smoke proves import/bundling only; first behavioral execution is the CI token spec below (if enabled) and P3b.
- **Attempt to re-enable one automated token-flow spec** (codex finding 3, time-boxed): rewrite `runTokenFlowAndAssert` for the standards token — **including the negative authorization case (Bob's `mint_to_private` reverts; Bob cannot move Alice's balance)** — and if the local-network CI job provides the accelerator (native bb → fast), un-skip the accelerated variant; else time-box ONE WASM measurement — > 4 min → leave skipped and the disposition becomes: **negative-assertion demand explicitly REJECTED for this cycle** (rationale: `authorize_once`/minter semantics source-verified; the demo's actual paths are covered by P3b + the P3d live-bundle pass; a permanently-skipped negative spec is worse than an honest rejection), ledgered with the measured number.
- kv-store path re-verify against installed node_modules (pre-verified from tarball).
- Lockfile: ONE local `bun install --minimum-release-age=0`. **Diff discipline (codex 2, tightened): review EVERY changed resolution + integrity — including `@aztec/*` transitive changes — not just non-@aztec roots.** Expected: @aztec 5.0.0→5.0.1 swaps + exactly one new root entry (standards, zero transitives — both auditors confirmed). Anything else = stop + `npm view <pkg>@<ver> time --json` (flag post-2026-07-09). Bun runs no lifecycle scripts for untrusted deps by default (no `trustedDependencies` entry added). Frozen-install proof.

**Validation gate:** override + frozen installs exit 0; zero skips + all `@aztec/*` pins `5.0.1` + standards pin present exactly once; **attestation subject digest == installed tarball integrity (recorded)**; lock-diff per the discipline above; `bun run test` exit 0 (lint + 3-graph playground typecheck + scripts-tsc + sdk + unit + **the new root `test:scripts` running the extended bump-tool test**); playground `build` exit 0; Playwright mocked (8) green; production smoke (2) green (proves standards import/bundling in the built bundle — load-time only); token CI spec disposition recorded (enabled & green incl. negative cases, or measured & explicitly rejected with the number); `CRS_CACHE_VERSION` = `5.0.1`. Layers: lint · typecheck ×3 · unit (incl. scripts) · UI-mock e2e · production-build (load) smoke.

### P2 ✓ — Land the PR — gate passed 2026-07-16 (PR #395 merged; all CI green incl. the ENABLED token spec + native-bb transmit; local download gate vs bb 5.0.1 PASSED)

- Branch → PR (bump commit + swap commit kept separate) → CI → auto-merge. No workflow edits expected beyond the bump tool script; `lint:actions` if any YAML moves.
- `sdk.yml` native-bb e2e (`build_accelerator: true`, sandbox auto-detects 5.0.1): asserts `transmit`.
- Local: `ACCELERATOR_DOWNLOAD_TEST=1 AZTEC_BB_VERSION=5.0.1 cargo test download_and_verify_bb -- --nocapture` from `packages/accelerator/core`.

**Validation gate:** PR CI green (sdk.yml transmit + app.yml incl. 3-graph typecheck + accelerator.yml + actionlint.yml + local-network job with whatever token-spec disposition P1 set); local download gate passes vs 5.0.1; PR auto-merges. Layers: lint · typecheck · unit · e2e (native bb 5.0.1) · download+digest.

### P3 — FPC redeploy → pre-publish smoke → publish `testnet` → acceptance (incl. live-bundle token pass) → promote `latest`

- **(a)** Fail-closed pre-flight (nodeVersion **expect `5.0.0` — correct this cycle**; l1ChainId 11155111; derived 5.0.1 salt-0 FPC `getContract` logged, RPC-error ≠ absent). Deploy `--salt 0x0`, `BRIDGE_AMOUNT=1000000000000000000000`; key via gitignored `scripts/.env` (recreate; delete at P3 end). Post-flight balance query in lessons.
- **(b)** Pre-publish smoke on `dev:testnet` (registry port; playground-local vite): account deploy + **standards-token flow** (deploy w/ minter, mint 1000, private transfer 500, balances 500/500 — same narrative assertion as today's demo). Gates the publish. **CONTINGENCY TRIGGER LIVES HERE**: standards leg fails & not quickly fixable → revert swap commit → re-run account+FPC leg → proceed with the pure security patch → re-land swap later via `skip_sdk_publish=true` deploy.
- **(c)** Dispatch `publish-testnet.yml --ref main` immediately post-merge, nothing queued (confirm the run started — one-queued-run caveat); record merge SHA. Publishes SDK `5.0.1` (`--provenance`), deploys playground.
- **(d)** Acceptance: npm tags (`testnet`=5.0.1, `latest` untouched); live bundle `5.0.1`; **registry-artifact test** (temp dir, install exact version, import + construct `AcceleratorProver`); **released-1.0.6 native smoke** (runtime-downloads bb 5.0.1; Playwright `local-network-access` grant); Safari (owner click-through) + WASM; **NEW (both auditors): one full standards-token flow on the DEPLOYED production bundle (WASM sufficient) — recorded in lessons — before any promote.** (~90s of testnet time; closes the dev/prod divergence class the sqlite3.wasm 404 proved real.)
- **(e)** Promote: `promote-latest.yml` `version=5.0.1`, same-day, only after (d) fully green incl. the live token pass.

**Validation gate:** pre-flight explicit; FPC deployed + balance recorded; pre-publish standards smoke (or contingency executed + documented) BEFORE (c); publish+deploy green; registry test passes; released-app native + Safari + WASM pass; **live-bundle token flow 500/500 recorded**; `npm view` `testnet` = `latest` = `5.0.1`. Layers: e2e-live-network (two stages incl. production-bundle behavior) · registry-artifact.

### P4 ⛔ BLOCKED (2026-07-16) — Deprecate `@alejoamiras/aztec-standards` — awaiting owner npm auth (`npm whoami` → 401; `npm owner ls` confirms alejoamiras owns it; the command is surfaced in chat — one login + one command + OTP)

- Pre-check (codex 7 + fable A4): `npm whoami` = `alejoamiras` AND `npm owner ls @alejoamiras/aztec-standards` includes them; expect an interactive OTP on write (2FA) — run interactively, never script the OTP.
- `npm deprecate @alejoamiras/aztec-standards "Moved to @aztec-foundation/aztec-standards — install that package instead."` (no range = all versions; reversible with an empty message).
- Verify **every version** carries the notice: `for v in $(npm view @alejoamiras/aztec-standards versions --json | jq -r '.[]'); do npm view "@alejoamiras/aztec-standards@$v" deprecated; done` — all non-empty. New-scope install still resolves.
- Auth via the user's local npm session only — NOT the CI `NPM_TOKEN` (least privilege; that token's purpose is accelerator publishing). Missing/insufficient auth → print the command, surface, hold.

**Validation gate:** all published versions return the deprecation message; `npm view @aztec-foundation/aztec-standards@5.0.1 version` resolves. Layers: registry state.

---

## Security & Adversarial Considerations

- **Supply chain — the new scope, stated on verifiable anchors (fable A1/codex 1 fold):** `@aztec-foundation/aztec-standards@5.0.1` was published **hours** before planning, from a repo **created 2026-07-13 as an AztecProtocol-org fork of `defi-wonderland/aztec-standards`** (the long-standing Wonderland implementation, 2024-12-30). What is verifiable and relied on: (1) canonical AztecProtocol GitHub org; (2) **SLSA provenance binding the publish to `release.yml` @ `refs/tags/v5.0.1`, commit `c74541f7…`** (independently re-fetched); (3) fork lineage from the known Wonderland codebase; (4) tarball integrity pinned in our lock; (5) zero install-time dependencies. What is NOT claimed: no audit evidence exists for the repo (an earlier draft said "audited" — withdrawn); provenance proves build-from-source, **not code review or bytecode semantics** (codex); the custody-handover story is user-attested context, not a registry-verifiable mitigation. Residual: a compromised Aztec-Foundation npm/CI could ship poisoned artifacts — same trust class as `@aztec/*`, accepted with the attestation-recording discipline in P1. **Also noted honestly:** "zero dependencies" is install-time only — the generated JS carries undeclared runtime imports of `@aztec/aztec.js/*`, resolved against OUR pins (the code executing is ours; the lockstep requirement is handled by the P1 tool extension).
- **Min-age override (4th use), tightened:** the override lifts the gate for the whole resolution; the diff discipline reviews EVERY changed resolution+integrity, not just new roots. Local-only, command-scoped, frozen CI, no lifecycle scripts for untrusted deps (bun default).
- **Token contract as demo bytecode:** same risk class as the current noir-contracts token (fable A5: auth hooks verifiably disabled at ZERO — no external call path; per-session random-salt deploys by throwaway accounts; sponsored fees; no user funds; artifact smaller than current). Front-run/reorg surface unchanged. FPC drainability analysis carries over (free-mint test FJ; exposure ≈ Sepolia gas).
- **Deprecation surface:** metadata-only + reversible, but install-time-visible text (social-engineering surface — message kept factual, exact package name only). Run under user-local auth with OTP, never CI tokens. **Ledgered residual (fable A4):** the deprecated package remains publishable by the personal account — a compromised `alejoamiras` account could ship a malicious "5.0.2" to existing dependents; out of scope, named.
- **npm `latest` flip:** smoke-gated (now incl. the live-bundle token pass) via the structurally-only promote path. Rollback: unlike last cycle, a good prior version exists (5.0.0) if 5.0.1 itself proves bad post-promote; fix-forward caveats unchanged.
- **L1 key + workflow hygiene:** unchanged inherited discipline; no workflow/permission/secret changes planned (bump-tool script is not CI-privileged).
- No `/harden` (user-confirmed; no new trust boundary).

## Assumptions

**Facts** (all independently re-verified by the fable audit unless noted):
- `@aztec/stdlib@5.0.1` npm 2026-07-15T19:15:34Z; GitHub v5.0.1 release 2026-07-15T20:00:20Z, 17 barretenberg assets, "required for aztec.nr/aztec.js/PXE, optional for node operators" verbatim.
- Live testnet at planning: `nodeVersion 5.0.0`, `rollupVersion 1821665230`, `l1ChainId 11155111`.
- Standards package: npm 2026-07-16T14:28:42Z (hours old at planning); provenance attested (2 attestations, independently re-fetched by main agent: npm-publish v0.1 + SLSA v1); binding per the Security section; maintainer `npm@aztec.foundation`; versions {5.0.1-rc.1, 5.0.1}; no deps/main/exports; `dist/src/artifacts/Token.{js,d.ts}` at the exact deep path; undeclared `@aztec/aztec.js/*` runtime imports; `config.aztecVersion: "5.0.1"`. Old package: latest 5.0.0, maintainer `alejoamiras`, registry repository metadata → `alejoamiras/ecosystem-tooling` (NOT the new repo — corrected from draft).
- Token API signatures verbatim from the 5.0.1 d.ts (constructor_with_minter / mint_to_private / transfer_private_to_private / balance_of_private / deployWithOpts). **`auth_contract=ZERO` semantics verified from the Noir source** (constructor docs + zero-guard on the hook).
- Repo: zero existing aztec-standards references; current token call sites `aztec.ts:589/:686/:708/:726/:744-745`; 24 pins; `update-aztec-version.ts:27` rewrites only `@aztec/`-prefixed keys (the P-4 blind spot); local-network token specs `test.skip`'d for CI time (`demo.local-network.spec.ts:43,:70` — "~7 min" note); mocked suite asserts UI-state only; production smoke is load-time only; `_e2e.yml` never touches tokens.
- kv-store 5.0.1 layout + imports map byte-identical to the vite machinery's expectations. SponsoredFPC sha `648b856a… → 15cf50ce…` (recomputed by both auditors independently).
- Released app 1.0.6; our npm `latest`=`testnet`=5.0.0; `5.0.1` base free. All inherited 5.0.0-cycle hardening verified on main (incl. `skip_sdk_publish` lever for the contingency).

**Inferences** (post-audit state):
- Deep-path import resolves under vite dev/build/tsc — **strengthened** (noir-contracts Token uses the byte-identical pattern through this exact pipeline; Bundler resolution permits it absent an exports map). Residual: dep-optimizer prebundling a new dep (precedented, fixable). Fallback (final-codex corrected): load the artifact JSON via `loadContractArtifact` and **deploy through the generic `Contract.deploy`** (+ `Contract.at` for reads) — `Contract.at` alone is not a deployment path.
- The token demo narrative (mint 1000 private → transfer 500 → balances 500/500) holds under `constructor_with_minter` + nonce-0 `authorize_once` self-call. *First behavioral execution: the P1 CI spec (if enabled) / P3b; asserted against the same 500/500 outcome; contingency covers failure.*
- 5.0.1 client on 5.0.0 node works (release framing; P3b proves pre-publish; failure → hold, node timing is upstream's).
- tsc clean beyond the token file (kv-store pre-verified layout-stable). *P1 confirms.*
- User's local npm auth suffices for P4 (whoami + owner ls + OTP). *Insufficient → surface-and-hold, gate-sanctioned.*

**Asks** — none open (Phase 0 pre-approvals cover min-age ×2 packages, same-day latest, both standards items, autonomy + 1000 FJ; P4's auth hold is mechanical, not a decision).

## Decision ledger

- **Chosen: A′** (single-shot + pre-authorized decoupling contingency; swap in its own commit). **Rejected: plain A** (fable: avoidable hostage scenario on a security release). **Rejected: B** (codex's preference; fable showed the contingency captures B's isolation at ~zero cost; B's "hours sooner" isn't worth a second dispatch cycle given P3b gates before any publish either way).
- **Codex round-1 (`reject`) dispositions:** (1) provenance overstated → **adopted** (trust chain restated on verifiable anchors; "audited"/"same repo" withdrawn; attestation recording in P1) — its full source-to-bytecode reproducibility demand **rejected as impractical for a bump cycle** (residual named; the attestation was also independently retrievable, resolving its "session testimony" concern). (2) resolution-wide min-age audit → **adopted** (review every changed resolution incl. @aztec transitives; bun's no-scripts default noted). (3) automated + post-deploy token coverage → **adopted** (time-boxed CI-spec re-enable attempt + the P3d live-bundle pass; the "before deprecation" ordering was already satisfied — P4 is last). (4) constructor/auth semantics → **adopted-stronger** (verified from Noir source by fable; sources repointed; incoherent fallback dropped; negative assertions folded into the CI spec if enabled). (5) old-token interop matrix under 5.0.1 client → **rejected with reason**: the playground has been `ephemeral: true` since the 5.0.0 cycle — no persisted 5.0.0 client state exists to interop with; the only reused on-chain state (FPC) holds no private notes and is redeployed; fable concurs ("negligible by construction"). (6) release-inventory completeness → **adopted** (#24645/#24479 + explicit N/A table). (7) P4 hardening → **adopted** (owner ls, all-versions verify, interactive OTP).
- **Fable (`conditional approve`) conditions:** all five **adopted** (contingency; live-bundle token pass; verification source repointed + fallback dropped; trust-chain restatement + attestation recording + undeclared-imports note; bump-tool lockstep extension in-cycle). Non-blocking items (#24403 line, gate wording, OTP nuance, old-package publishability residual) — **all adopted**.
- **Final fresh-context codex pass (`conditional approve`) — all 5 conditions folded:** contingency mechanics repaired (it had NOT actually freed the security channel — revert is a PR, and P3d/e now relax to the old-token flow while the revert holds); attestation subject-digest ↔ tarball-integrity comparison made mandatory (was two disconnected recordings); scripts tests wired into a real gate (root `test:scripts` + sdk.yml step + `scripts/**` path trigger); negative-coverage disposition made honest (in-spec if enabled, explicit rejection otherwise); `Contract.deploy` fallback + P4-hold-is-BLOCKED semantics fixed. It also independently endorsed the two round-1 rejections (old-state interop; reproducibility trade).
- **Disputed/unresolved:** none — the A/B split was the only material disagreement and the A′ hybrid (as operationalized) resolves it with all three reviewers' inputs.
- **Named follow-ups:** none new beyond inherited (CRS derivation, FPC script fail-closed, OIDC trusted publisher).

## Post-implementation hardening

None scheduled (no new trust boundary; 4th consecutive cycle).

## Codex audit (mid, xhigh) — round 1: `reject` → all findings adopted or dispositioned (see ledger). Transcript: `audit-codex.md`.
## Fable audit (mid, Fable 5) — `conditional approve`, all 5 conditions folded (independently re-derived every load-bearing Fact; several strengthened). Transcript: `audit-fable.md`.
## Final fresh-context codex pass (mid, xhigh) — `conditional approve`, all 5 conditions folded

Session `019f6b9c-e640-7483-a18d-9369a3ae21cd` (fresh context; saw plan + both transcripts + ledger). Verdict `conditional approve` with 5 conditions, all fold-repairs: (1) contingency mechanics operationalized (revert-PR ordering, old-token gates while reverted, standards requirement re-attaches at the re-land); (2) attestation subject digest cryptographically compared to the installed tarball + workflow skim; (3) scripts tests made a real gate (root `test:scripts` + sdk.yml wiring/path trigger); (4) the automated/negative-coverage disposition made honest (in the spec if enabled; explicit rejection otherwise); (5) fallback semantics (`Contract.deploy`, not `.at`) + P4 hold = BLOCKED, never ✓. Confirmed: the old-state-interop rejection is sound; the reproducibility trade proportionate. Transcript appended to `audit-codex.md`.

## Seeds (FINAL — approved 2026-07-16 unconditionally; drafts unchanged = canonical)

**Recommended `/loop 15m`:**

```
/loop 15m Drive implementations-plan/aztec-5.0.1-2026-07-16 to completion (P1–P4); never idle. Each firing: read plan.md + lessons/; git status + log; PR → gh pr view --json statusCheckRollup; CI in-flight → gh run watch up to 10m. P1: aztec:update 5.0.1 (zero skips; Windows sha vs release digest) → EXTEND update-aztec-version.ts for @aztec-foundation/aztec-standards lockstep + unit test + NEW root test:scripts (`bun test scripts/`) wired into root test:unit AND sdk.yml (step + scripts/** path trigger) → add standards pin (exact) → ATTESTATION VERIFY: re-fetch attestation, COMPARE subject digest == installed tarball integrity (mismatch = STOP), skim release.yml @ c74541f7…, record all in lessons → TOKEN SWAP AS ITS OWN COMMIT (deep-path import; constructor_with_minter(…, alice, AztecAddress.ZERO) — source-verified; transfer_private_to_private(alice,bob,500n,0)) → attempt re-enable of one local-network token spec INCLUDING negative cases (Bob mint reverts; Bob can't move Alice's balance) — accelerated if the job has the accelerator; else time-box ONE WASM run, >4min → leave skipped + ledger as explicit rejection with the number → kv-store path re-verify → ONE local `bun install --minimum-release-age=0` → lock-diff: review EVERY changed resolution incl. @aztec transitives (expect swaps + exactly one new zero-transitive root) → frozen proof → full P1 gate. P2: PR (commits kept separate) → CI (sdk.yml transmit) → local ACCELERATOR_DOWNLOAD_TEST=1 AZTEC_BB_VERSION=5.0.1 cargo test download_and_verify_bb (packages/accelerator/core) → auto-merge. P3: (a) pre-flight (nodeVersion 5.0.0 EXPECTED, chainId 11155111, getContract logged, RPC-error ≠ absent) → scripts/.env recreated (never inline/print; DELETE at P3 end) → deploy-sponsored-fpc.ts --salt 0x0 BRIDGE_AMOUNT=1000000000000000000000 → balance query in lessons; (b) PRE-PUBLISH SMOKE dev:testnet: account deploy + STANDARDS token 1000→500/500 — MUST PASS BEFORE PUBLISH; CONTINGENCY (operational): standards leg fails & not quickly fixable → REVERT PR (git revert the swap commit → CI → merge) → re-run P3b as the OLD-token flow → publish/accept/promote 5.0.1 with OLD-token live-bundle pass (standards requirement re-attaches only at the re-land) → later: revert-the-revert PR → merge → skip_sdk_publish=true playground-only dispatch → standards production smoke → only then P4; (c) dispatch publish-testnet.yml --ref main post-merge, nothing queued, confirm started, record SHA (AUTONOMOUS); (d) npm tags + live bundle 5.0.1 → registry-artifact test → released-1.0.6 native smoke (grant local-network-access) + Safari (owner click-through — ask, don't skip) + WASM → LIVE-BUNDLE STANDARDS TOKEN FLOW 500/500 recorded in lessons — REQUIRED BEFORE PROMOTE; (e) once (d) fully green, SAME DAY, dispatch promote-latest.yml version=5.0.1 (AUTONOMOUS). P4: npm whoami == alejoamiras AND npm owner ls includes them → npm deprecate @alejoamiras/aztec-standards "Moved to @aztec-foundation/aztec-standards — install that package instead." (interactive OTP; never script it) → verify EVERY version shows the notice → new-scope install resolves; auth insufficient → print command, mark P4 BLOCKED in plan.md (never ✓), SURFACE AND HOLD. Stuck/trade-off → /codex xhigh, log consult. Phase green = plan.md gate passes → ✓ + lessons + print LESSONS_FILE=… → advance. 5× same-step fail → stop, reassess w/ codex. All ✓ → /code-review max --fix → separate commit → codex post-impl audit → address high/critical → wrap-up → stop. HARD LIMITS: never push main; never print the L1 key or npm tokens/OTPs; never expand scope; holds = failed gates, P4 auth, accelerator-interface escalation.
```

**Alternative `/goal`:**

```
/goal All P1–P4 phases marked ✓ in implementations-plan/aztec-5.0.1-2026-07-16/plan.md (per-phase headers), each ✓ backed by its gate passing in the transcript; LESSONS_FILE printed per phase; attestation binding + lock integrity recorded in P1 lessons; attestation subject digest verified == installed tarball integrity in the transcript; pre-publish standards-token smoke passing BEFORE the publish dispatch (or the decoupling contingency executed per its operational steps and documented); npm dist-tags testnet = latest = 5.0.1 in the transcript; 5.0.1 salt-0 FPC getContract defined with balance recorded; registry-artifact test passing; released-1.0.6 native + Safari + WASM passing; LIVE-BUNDLE token flow 500/500 (standards — or old-token if the contingency fired) recorded before the promote dispatch; P4 ✓ ONLY with every @alejoamiras/aztec-standards version returning the deprecation message — an auth hold means P4 marked BLOCKED, surfaced, and the goal NOT met; /code-review max --fix applied+committed; codex post-impl high/critical addressed; bun run test and bun run lint:actions exit 0. Constraints: never push main; never print the L1 key or OTPs; BRIDGE_AMOUNT=1000 FJ; holds only for failed gates, P4 auth, or accelerator-interface escalation.
```
