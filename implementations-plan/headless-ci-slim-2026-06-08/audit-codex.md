**Verdict: A; holds-with-changes.** Your provisional pick is not wrong, but the plan overclaims a few “verified” and “security” points, and it misses one real Smoke semantic change: `bb_available` will likely flip false.

- **1. A vs B:** Prefer **A**, not B. The current delta is only two optional behaviors inside the shared setup action: Linux GUI libs and `copy-bb.ts` prebuild ([`.github/actions/setup-accelerator/action.yml:64`](</Users/alejoamiras/Projects/aztec-accelerator/.github/actions/setup-accelerator/action.yml:64>), [`:94`](</Users/alejoamiras/Projects/aztec-accelerator/.github/actions/setup-accelerator/action.yml:94>)). Duplicating Bun install, Rust toolchain, and rust-cache into a second composite is classic drift bait; B only becomes better if headless setup is expected to diverge materially beyond this PR. The one fix I’d require if you choose A: tie the host/target assertion to `run-prebuild=true`, because that guard exists for sidecar selection, not for pure headless builds ([`:42`](</Users/alejoamiras/Projects/aztec-accelerator/.github/actions/setup-accelerator/action.yml:42>)).

- **2. What breaks if you drop WebKit/GTK + prebuild?** The **build** should not break. `Smoke` and `Release Smoke` build `../server/Cargo.toml`, not the Tauri crate ([`.github/workflows/accelerator.yml:143`](</Users/alejoamiras/Projects/aztec-accelerator/.github/workflows/accelerator.yml:143>), [`:219`](</Users/alejoamiras/Projects/aztec-accelerator/.github/workflows/accelerator.yml:219>)); `accelerator-server` depends only on `accelerator-core` plus tokio/tracing ([`packages/accelerator/server/Cargo.toml:16`](</Users/alejoamiras/Projects/aztec-accelerator/packages/accelerator/server/Cargo.toml:16>)), and `accelerator-core` has no Tauri/GTK tree ([`packages/accelerator/core/Cargo.toml:17`](</Users/alejoamiras/Projects/aztec-accelerator/packages/accelerator/core/Cargo.toml:17>)). But the plan overstates the tripwire: the denylist check in Smoke proves “no known Rust GUI/TLS-serving crates,” not “no native GUI build deps” in the abstract ([`.github/workflows/accelerator.yml:147`](</Users/alejoamiras/Projects/aztec-accelerator/.github/workflows/accelerator.yml:147>)). The real behavioral change is `/health.bb_available`: without prebuild and without `BB_BINARY_PATH`, `find_bb(None)` will usually fail, so `/health` likely becomes `bb_available=false` even though `status` stays `ok` ([`packages/accelerator/core/src/server.rs:164`](</Users/alejoamiras/Projects/aztec-accelerator/packages/accelerator/core/src/server.rs:164>), [`packages/accelerator/core/src/bb.rs:12`](</Users/alejoamiras/Projects/aztec-accelerator/packages/accelerator/core/src/bb.rs:12>)).

- **3. Version-only extraction from `copy-bb.ts`:** Safe **if and only if** the real prebuild keeps calling the same extracted resolver. The resolver itself is cleanly isolated today at [`copy-bb.ts:144`](</Users/alejoamiras/Projects/aztec-accelerator/packages/accelerator/scripts/copy-bb.ts:144>)–150, and the checksum/fetch path is elsewhere. But the plan is wrong that the current “supply-chain gate already exercises the resolution”: the existing tests only cover Windows tag/checksum/SHA helpers, not the `Bun.resolveSync("@aztec/bb-prover") -> @aztec/bb.js/package.json` chain ([`copy-bb.test.ts:11`](</Users/alejoamiras/Projects/aztec-accelerator/packages/accelerator/scripts/copy-bb.test.ts:11>)). Add a direct test for the resolver or you are introducing an untested drift point.

- **4. Supply-chain gate / security:** The Security section is overstated. The pinned SHA-256 gate exists only for the **Windows** fetch path ([`copy-bb.ts:56`](</Users/alejoamiras/Projects/aztec-accelerator/packages/accelerator/scripts/copy-bb.ts:56>), [`:89`](</Users/alejoamiras/Projects/aztec-accelerator/packages/accelerator/scripts/copy-bb.ts:89>), [`:159`](</Users/alejoamiras/Projects/aztec-accelerator/packages/accelerator/scripts/copy-bb.ts:159>)); on Linux/macOS the script just copies `bb` out of installed `@aztec/bb.js` ([`:161`](</Users/alejoamiras/Projects/aztec-accelerator/packages/accelerator/scripts/copy-bb.ts:161>)). So extracting a version-only resolver does **not** weaken the pinned-checksum gate for the real Windows copy path, but the writeup should stop saying that “the actual-bb integrity gate stays on for every leg that uses bb.” Cross-platform, the trust anchor is `bun install --frozen-lockfile`, not an in-repo checksum.

- **5. Release-Smoke parity:** Dropping prebuild from `Release Smoke` is technically safe because that job only builds the headless binary and tars/checksums it; it never runs `/health`, never needs sidecar placement, and never sets `BB_BINARY_PATH` ([`.github/workflows/accelerator.yml:213`](</Users/alejoamiras/Projects/aztec-accelerator/.github/workflows/accelerator.yml:213>)–227). The caveat is different: PR `Release Smoke` is only archive-shape parity, not true release parity. The real release workflow patches versions and asserts `accelerator-server --version` before packaging ([`.github/workflows/release-accelerator.yml:270`](</Users/alejoamiras/Projects/aztec-accelerator/.github/workflows/release-accelerator.yml:270>)–313), so don’t oversell what `Release Smoke` proves.

- **6. e2e `AZTEC_VERSION` export fragility:** The hook is fine, but only if it happens in the **same step as launch** or is written to `$GITHUB_ENV`. `_e2e.yml` launches the server in a later step ([`.github/workflows/_e2e.yml:80`](</Users/alejoamiras/Projects/aztec-accelerator/.github/workflows/_e2e.yml:80>)); a plain `export` in an earlier step will not persist. The path itself is stable today because prebuild writes `packages/accelerator/src-tauri/AZTEC_VERSION` ([`copy-bb.ts:181`](</Users/alejoamiras/Projects/aztec-accelerator/packages/accelerator/scripts/copy-bb.ts:181>)) and `_e2e.yml` still runs prebuild immediately before build ([`.github/workflows/_e2e.yml:51`](</Users/alejoamiras/Projects/aztec-accelerator/.github/workflows/_e2e.yml:51>)–57). One last nit: the proposed `/health.version` assert is low-signal on PR CI, because headless explicitly injects `app_version = env!("CARGO_PKG_VERSION")` and the `/health` fallback is the same value today ([`packages/accelerator/server/src/main.rs:67`](</Users/alejoamiras/Projects/aztec-accelerator/packages/accelerator/server/src/main.rs:67>), [`packages/accelerator/core/src/server.rs:167`](</Users/alejoamiras/Projects/aztec-accelerator/packages/accelerator/core/src/server.rs:167>)). It’s still fine as a shape guard, just not a strong semantic one.