Clean — no implementation defects found in Part A or Part B.

No findings.

- Part A is aligned with the real `_e2e` leg. In `.github/workflows/_e2e.yml:45-55`, the accelerator path now does only `apt`, `copy-bb`, and then `cargo build` from `packages/accelerator/server` at `.github/workflows/_e2e.yml:83-88`. That server crate depends on `accelerator-core`, not `src-tauri`/Tauri (`packages/accelerator/server/Cargo.toml:16-24`), so there is no WebKit/GTK consumer left in this leg. Keeping `libssl-dev` is still justified: the headless tree pulls `reqwest` in `packages/accelerator/core/Cargo.toml:17-31`, and the lock still includes `native-tls`/`openssl-sys` in the server graph (`packages/accelerator/server/Cargo.lock:895-964`). I do not see any omitted Linux lib beyond that.

- Part B’s new composite flags are correct. In `.github/actions/setup-accelerator/action.yml:38-48` and `:79-90`, the gates compare string inputs against `'false'`, so `install-tauri-system-deps: "false"` and `run-prebuild: "false"` are the right spellings. `install-tauri-system-deps: "false"` is harmless on macOS because the only affected steps are Linux-only (`runner.os == 'Linux'`). `run-prebuild: "false"` skips the host/target assert and bb-copy, but not target installation: `dtolnay/rust-toolchain` still runs unconditionally with `targets: ${{ inputs.rust-target }}` at `.github/actions/setup-accelerator/action.yml:105-108`.

- The release-file cleanup also matches the code. The deleted `src-tauri/Cargo.toml` patch was dead for `build-headless`; the headless server injects `app_version: Some(env!("CARGO_PKG_VERSION"))` in `packages/accelerator/server/src/main.rs:62-72`, and `/health.version` reads that injected value in `packages/accelerator/core/src/server.rs:163-168`. The rewritten `--locked` rationale is factual: `packages/accelerator/server/Cargo.lock:30-39` contains the root `accelerator-server` package entry, so patching `server/Cargo.toml` is what makes that lock intentionally stale. With the `src-tauri` sed gone, only `packages/accelerator/server/Cargo.toml.bak` is created, so narrowing the cleanup rm is correct.

Residual risk: none new that the implementation introduced. The only caveat remains the already-known one from the plan: `run-prebuild: "false"` also removes the host==target assert, so future runner-label drift would be caught less explicitly. For this job, that is a maintainability concern, not a present correctness issue.