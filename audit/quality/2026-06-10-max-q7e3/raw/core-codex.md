1. `ProveError` is a protocol, not a type — `Data Clump` + `Duplicate Code`. Impact: `structural`; blast radius: `server.rs`, `server/auth.rs`, `server/prove.rs`; change frequency: high (`server.rs` HOT). Instances: `server.rs:312-327`; `server/auth.rs:41-47,67-73,79-85,99-101,105-111,132-138`; `server/prove.rs:63-69,122-128,134-137,182-188,222-224` (11 call sites, not ~17). Why harder: adding a field like `request_id`, standardizing status mapping, or changing the error media type requires hand-editing every `(StatusCode, String)` branch and keeping status/body shape aligned. Smallest safe refactor: `Replace Error Tuple with Type` (`AppError`/`ProveError` implementing `IntoResponse`). Disappears: repeated tuple construction, repeated `json_error(...)`, and handler-level knowledge of serialization details.

2. `versions` is a policy + cache + GitHub + layout hub — `Large Module` + `Divergent Change`. Impact: `architectural`; blast radius: `versions/mod.rs`, `versions/downloader.rs`, plus callers `bb.rs` and `server/prove.rs`; change frequency: medium-high. Instances: `versions/mod.rs:11-18,23-68,80-156,165-197,218-274,297-377`; `versions/downloader.rs:8-58,116-212`. Why harder: a retention-policy tweak, cache-dir layout change, GitHub API change, or platform naming change all queue on the same module boundary, so unrelated work collides and reviewers must reload the whole subsystem. Smallest safe refactor: `Extract Module` into `version_policy`, `cache_layout`, and `release_metadata`, with `downloader` depending on explicit helpers. Disappears: `downloader.rs` reaching broadly into `super::{...}` for unrelated concerns. Note: tier retention is already centralized in `NetworkTier::retention_limit` (`versions/mod.rs:61-68`), so this is not primarily a `Switch Statements` smell.

3. `AuthorizationManager` maintains two indexes by discipline — `Temporal Coupling`. Impact: `structural`; blast radius: `authorization.rs`, `server/auth.rs`; change frequency: medium. Instances: `authorization.rs:172-176,213-217,224-231,240-241`. Why harder: adding cancellation, expiry, observability, or alternate lookup paths means every mutator must update `by_origin` and `by_request` in lockstep; one missed path leaves stale piggybacking or unresolvable requests. Smallest safe refactor: `Extract Class` (`PendingAuthorizations`) or `Introduce Bidirectional Map` with single-purpose mutators. Disappears: manual sync logic between `by_origin` and `by_request`.

4. `AztecVersion` exists, but hot paths still fall back to raw strings — `Primitive Obsession` (partial `Replace Primitive with Object`). Impact: `structural`; blast radius: `server.rs`, `server/prove.rs`, `bb.rs`, `versions/mod.rs`, `versions/downloader.rs`; change frequency: high on the prove/version path. Instances: `server.rs:37,87`; `server/prove.rs:38-40,74-89,140-159`; `bb.rs:18,27-32,75-81`; `versions/mod.rs:154-156,191-197,356-371`; `versions/downloader.rs:21-22,31,37,54,116,156`. Why harder: if version handling grows beyond “one raw string” (display tag vs cache key vs release tag, or stricter bundled-version invariants), every `&str` seam must be re-audited. Smallest safe refactor: `Change Function Signature` to `&AztecVersion` for `find_bb`, `bb::prove`, `version_bb_path`, `download_url`, and `cleanup_old_versions` (plus a small wrapper for bundled/default version state). Disappears: repeated raw/typed conversion, string comparisons, and caller-managed invariants.

**Refuted**
- `server.rs` being 1424 LOC is not, by itself, the strongest `Large Class` finding: production code is `server.rs:1-328`; `server.rs:330-1424` is test colocation.
- The `bb` priority ladder in `bb.rs:18-63` is centralized and short; the maintainability issue there is the raw version-string seam around it, not the ladder itself.