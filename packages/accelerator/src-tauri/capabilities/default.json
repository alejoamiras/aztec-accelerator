{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the accelerator. NOTE: `updater:default` is deliberately NOT granted — F-004 requires all updates to flow through our Rust check_for_update/perform_update, which verify the signed manifest envelope + monotonic version floor. Granting the raw updater plugin commands to the webview (check/download/install/download_and_install) would let a compromised frontend bypass that verification entirely.",
  "platforms": ["linux", "macOS", "windows"],
  "permissions": [
    "core:default",
    "autostart:allow-disable",
    "autostart:allow-is-enabled",
    "process:default"
  ]
}
